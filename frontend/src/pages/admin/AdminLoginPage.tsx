import { AppLayout } from "../../components/layout/AppLayout";

export function AdminLoginPage() {
  return (
    <AppLayout
      title="Login Administrativo"
      description="Autenticação de usuários humanos (SUPER_ADMIN, ADMIN_RESTAURANTE) será implementada nas próximas tasks."
    />
  );
}
