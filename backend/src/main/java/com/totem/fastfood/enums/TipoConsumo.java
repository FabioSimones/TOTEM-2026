package com.totem.fastfood.enums;

/**
 * Indica o destino do pedido escolhido pelo cliente no totem.
 * Usado para organização e possível diferenciação futura de embalagem.
 */
public enum TipoConsumo {

    /** Cliente irá consumir no local (comer aqui). */
    LOCAL,

    /** Cliente irá levar o pedido (para viagem). */
    VIAGEM
}
