# Status do MVP — Totem Fast Food

Criado na TASK-081 (Fase 13 — Consolidação de testes e qualidade). Consolida o estado real do projeto após 81 tasks, cruzando documentação, código e resultados de teste — não substitui os documentos detalhados (`docs/08-endpoints.md`, `docs/09-contratos-api.md`, `docs/testes-backend-mvp.md`, `docs/checklists/*`), serve como visão executiva de "onde o projeto está agora".

**Para priorização e próximos passos pós-MVP, ver `docs/roadmap-pos-mvp.md` (TASK-095)** — este documento continua registrando o histórico de tasks concluídas; o roadmap é quem organiza P0/P1/P2/P3 e a sequência recomendada daqui em diante.

## Visão geral

Sistema de autoatendimento para fast food: Totem (cliente monta pedido e paga), Caixa (confirma dinheiro, envia à cozinha, retira), Cozinha (prepara), e um Painel Administrativo (gestão de restaurantes, cardápio, dispositivos, usuários, pedidos e um dashboard operacional). Backend Java 21 + Spring Boot 3.3.5 + PostgreSQL + Flyway; frontend React 19 + TypeScript + Vite, sem framework de UI.

## Módulos concluídos

### Backend

| Módulo | Controller | Service | DTOs | Teste dedicado | Documentado |
|---|---|---|---|---|---|
| Autenticação (login/refresh/logout/rate limit) | `AuthController` | `AuthService`, `RefreshTokenService`, `LoginAttemptService` | ✅ | ✅ (`AuthRefreshLogoutTest`, `AuthLoginRateLimitTest`, `LoginAttemptServiceTest`, `RefreshTokenServiceTest`) | ✅ |
| Autenticação de dispositivo | `DispositivoAuthController` | `DispositivoService.ativarComCodigo` | ✅ | ✅ (via `DispositivoServiceTest`/`FluxoOperacionalMvpIntegrationTest`) | ✅ |
| Restaurantes | `RestauranteAdminController` | `RestauranteService` | ✅ | ✅ (`RestauranteServiceTest`, **novo na TASK-081** — módulo não tinha nenhum teste até então) | ✅ |
| Categorias | `CategoriaAdminController` | `CategoriaService` | ✅ | ✅ (`CategoriaServiceTest`) | ✅ |
| Produtos | `ProdutoAdminController` | `ProdutoService` | ✅ | ✅ (`ProdutoServiceTest`) | ✅ |
| Usuários | `UsuarioAdminController` | `UsuarioService` | ✅ | ✅ (`UsuarioServiceTest`) | ✅ |
| Dispositivos (cadastro/status operacional) | `DispositivoAdminController` | `DispositivoService`, `DispositivoAcessoService` | ✅ | ✅ (`DispositivoServiceTest`, `DispositivoAcessoServiceTest`, `DispositivoMapperTest`, `DispositivoAcessoIntegrationTest`) | ✅ |
| Upload de imagem | `UploadAdminController` | `UploadImagemService` | ✅ | ✅ (`UploadImagemServiceTest` + `UploadAdminIntegrationTest`, **HTTP/autorização adicionado na TASK-082**) | ✅ |
| Pedidos (listagem/detalhe paginado) | `PedidoAdminController` | `PedidoAdminService` | ✅ | ✅ (`PedidoAdminIntegrationTest`) | ✅ |
| Expiração de pedidos | `PedidoExpiracaoAdminController` | `PedidoExpiracaoService` | ✅ | ✅ (`PedidoExpiracaoServiceTest`, casos em `PedidoAdminIntegrationTest`, `PedidoExpiracaoPostgresIT` **contra Postgres real, TASK-083**) | ✅ |
| Dashboard | `DashboardAdminController` | `DashboardAdminService` | ✅ | ✅ (`DashboardAdminIntegrationTest`) | ✅ |
| Fluxo Totem/Caixa/Cozinha | `PedidoTotemController`, `CardapioTotemController`, `CaixaPedidoController`, `CaixaPagamentoController`, `CozinhaPedidoController` | `PedidoTotemService`, `PagamentoTotemService`, `CardapioTotemService`, `CaixaPedidoService`, `CozinhaPedidoService` | ✅ | ✅ (`CaixaPedidoServiceTest`, `CozinhaPedidoServiceTest`, `FluxoOperacionalMvpIntegrationTest`, `FakePaymentProviderTest`) | ✅ |

Padronização de fuso horário (UTC) implementada e validada na TASK-079 (`TotemApplication`, `ClockConfig`, `TimezoneIntegrationTest`).

### Frontend

- Totem, Caixa, Cozinha — fluxo operacional completo, com polling leve no Totem.
- Ativação de dispositivo (`/ativar-dispositivo`).
- Admin: login, dashboard, restaurantes, categorias, produtos, usuários, dispositivos (com status operacional e filtros), pedidos (paginado, com correção de página vazia).
- Refresh token automático, logout, rate limiting tratado na UI.
- Escopo por restaurante refletido visualmente (sem duplicar a segurança real, que é 100% backend).
- Utilitário central de data/hora (`utils/dateTime.ts`, TASK-080) — corrige a interpretação de `LocalDateTime` UTC do backend em todas as telas administrativas, Caixa e Cozinha.

## Validações executadas

**TASK-081 (consolidação inicial)**:
- `mvn test` completo (Maven via `~/.m2/wrapper/dists`, `mvn` não está no `PATH` deste ambiente): **224/224 testes, BUILD SUCCESS** (215 herdados + 9 novos de `RestauranteServiceTest`).
- `npm run build`: sem erro TypeScript.
- `npx oxlint` (via `./node_modules/.bin/oxlint`): **1 warning pré-existente**, não relacionado a esta task — `src/contexts/ThemeContext.tsx:28` (`react/only-export-components`, Fast Refresh) — não corrigido por exigir separar o hook em outro arquivo, uma mudança de arquitetura fora do escopo de "ajuste pequeno e objetivo".
- Inventário completo de endpoints (`grep` de `@*Mapping` em todos os controllers) comparado contra `docs/08-endpoints.md` — **nenhuma divergência encontrada** (nenhum endpoint documentado ausente no código, nenhum endpoint implementado sem documentação).
- Busca por `new Date(`/`toLocaleDateString`/`toLocaleString`/`toLocaleTimeString` no frontend — **só existe dentro de `utils/dateTime.ts`**, confirmando que a correção da TASK-080 não deixou nenhum ponto direto remanescente.
- Busca por `TODO`/`FIXME`/`XXX` em backend e frontend — nenhuma ocorrência.

**TASK-082 (fecha a pendência de uploads)**:
- `mvn test -Dtest=UploadAdminIntegrationTest` → 9/9 testes.
- `mvn test` completo → **233/233 testes, BUILD SUCCESS** (224 anteriores + 9 novos).
- Confirmado que nenhum arquivo de teste vazou para `backend/target/test-uploads` ou `backend/uploads` reais — o teste isola `app.uploads.dir` num `@TempDir` próprio via `@DynamicPropertySource`.
- Nenhum bug de produção encontrado — autorização, multipart e acesso público já se comportavam exatamente como documentado.

**TASK-083 (Testcontainers/PostgreSQL real)**:
- `mvn test` (H2, sem o profile) → **233/233, BUILD SUCCESS**, inalterado — confirma que a nova suíte Postgres não interfere na suíte padrão.
- `mvn verify -Ppostgres-it` (Postgres 16 real via Testcontainers) → **5/5, BUILD SUCCESS** — `TimezonePostgresIT` (2 testes) + `PedidoExpiracaoPostgresIT` (3 testes), migrations Flyway reais aplicadas contra o container.
- Confirmado via `docker ps -a` que nenhum container de teste ficou órfão após a execução.
- Nenhum bug de produção encontrado — os dois pontos sensíveis (fuso horário, expiração) já se comportam corretamente contra Postgres real, confirmando que as correções da TASK-079 realmente resolveram os bugs na origem, não só na suíte H2.

**TASK-084 (CI GitHub Actions)**:
- Criado `.github/workflows/ci.yml` com 3 jobs paralelos: `backend-h2` (`mvn test`), `backend-postgres-it` (`mvn verify -Ppostgres-it`, Docker nativo nos runners Ubuntu), `frontend` (`npm ci && npm run build && npm run lint`).
- Validado localmente (o workflow em si não roda fora do GitHub): `mvn test` → 233/233; `mvn verify -Ppostgres-it` → 5/5; `npm run build` → sem erro TypeScript; `npx oxlint` → exit code 0, mesmo warning pré-existente de `ThemeContext.tsx`.
- Nenhum bug de produção encontrado — task de DevOps/CI pura, nenhum código de produção alterado.

**TASK-085 (bug de CORS — login SUPER_ADMIN pelo frontend)**:
- **Bug real de produção encontrado e corrigido**: `SecurityConfig` nunca teve configuração de CORS — nenhuma origem de frontend era liberada, bloqueando toda chamada feita pelo navegador (não só uma porta específica). Corrigido com `corsConfigurationSource()` liberando `http://localhost:5173`/`5174` (portas de desenvolvimento do Vite), habilitado via `.cors(Customizer.withDefaults())`.
- `curl` sem `Origin` já retornava `200` com tokens corretos antes da correção — confirmou que credencial/seed/rate-limit nunca foram o problema.
- Após a correção: preflight `OPTIONS` e `POST /api/auth/login` com `Origin: http://localhost:5174` → `200` com `Access-Control-Allow-Origin` correto; `mvn test` → 233/233; `npm run build` → sem erro TypeScript.
- **Login SUPER_ADMIN confirmado funcionando pelo navegador real** (primeira validação de clique real desde a TASK-060 — ver pendência abaixo).
- Melhoria de UX incluída: `autoComplete="email"`/`autoComplete="current-password"` em `AdminLoginPage.tsx`.

**TASK-086 (validação real da UI Admin no navegador)**:
- Com o CORS corrigido na TASK-085, executado o roteiro completo de clique real (não `curl`) nas 7 telas administrativas principais: login SUPER_ADMIN, Admin Home, Dashboard, Pedidos, Dispositivos, Produtos, Categorias, Restaurantes, Usuários — mais login `ADMIN_RESTAURANTE` (escopo preservado, 403 sem derrubar sessão) e renovação automática de sessão via refresh token (accessToken inválido + refreshToken válido → renova sozinho; ambos inválidos → limpa sessão e volta ao login).
- Revisão de código de todas as 7 páginas Admin antes do clique real, sem encontrar bugs.
- `mvn test` → 233/233; `npm run build` → sem erro TypeScript. **Nenhum bug de produção encontrado** — nenhuma alteração de código nesta task.
- Ver `docs/checklists/admin-mvp.md` seção 11 para o detalhamento completo por tela.

**TASK-116 (validação e homologação da autenticação centralizada)**:
- A centralização da autenticação (login único `/login`, `AuthProvider`, `resolveHomeRoute`, `ProtectedRoute`/`RoleGuard`, storage separado USER/DEVICE/OPERADOR, correção do `JwtAuthenticationFilter` contra o token `OPERADOR` como Bearer) tinha sido implementada em código numa task anterior, mas sem nenhum build/teste/lint/E2E executado. Esta task executou tudo de verdade e corrigiu a única regressão encontrada.
- Revisão estática: nenhuma referência viva a `setAccessToken`/`getAccessToken`/`setRefreshToken`/`clearSession`/chaves legadas de storage fora do bloco de migração e dos testes de migração; todos os consumidores usam `saveUserSession`/`saveDeviceSession`/`saveOperadorSession`/`clearUserSession`/`clearDeviceSession`/`clearOperadorSession`; nenhum guard manual duplicado restante nas páginas `/admin/*`; `X-Operador-Token` só é anexado em `/api/caixa/**`/`/api/cozinha/**`.
- `mvn test` → **354/354 testes, BUILD SUCCESS**, 0 falhas, 0 erros (37 classes, incluindo os 9 arquivos de teste de autenticação/segurança). Nenhuma correção necessária no backend.
- `npm test` (Vitest) → **142/142 testes, 23 arquivos**, 0 falhas — incluindo 30 testes novos desta task (`AuthProvider.test.tsx`, `ProtectedRoute.test.tsx`, `RoleGuard.test.tsx`, `routeResolver.test.ts`, `LoginPage.test.tsx`, `TotemHomePage.test.tsx`, mais casos adicionados a `tokenStorage.test.ts`) cobrindo isolamento de sessão, `resolveHomeRoute` para operadores com/sem dispositivo compatível, Totem com dispositivo incompatível e proteção contra open redirect.
- `npm run build` → sem erro TypeScript. `npx oxlint .` → exit 0, 2 warnings pré-existentes/consistentes (`react/only-export-components` em `ThemeContext.tsx` e, agora também, em `AuthProvider.tsx` pelo mesmo padrão — contexto e componente no mesmo arquivo).
- **Regressão real encontrada e corrigida**: `e2e/acessibilidade.spec.ts` ("navegação por Tab mostra foco visível no login administrativo") falhava porque `/admin/login` passou a ser um redirect client-side (`<Navigate>`) para `/login` — `page.goto()` resolve antes do redirect + montagem do formulário acontecerem, então `Tab` podia ser pressionado com `document.activeElement` ainda em `<body>`. Corrigido aguardando o campo "E-mail" existir antes de pressionar Tab.
- `npx playwright test` (suíte mockada) → **30/30 passando** (21 pré-existentes + 9 novos em `e2e/login-centralizado.spec.ts`, cobrindo `ADMIN_RESTAURANTE`, preservação da rota original pós-login, operadores sem dispositivo compatível, operador com dispositivo compatível, Totem com dispositivo incompatível, isolamento de logout/troca de dispositivo entre sessões USER e DEVICE, e confirmação de que o token de operador nunca vai no header `Authorization`).
- `npm run e2e:integrado` → **não executado** (pendência justificada): o backend real já estava de pé com CORS liberado para `127.0.0.1:5174`, mas a suíte grava dados reais via `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD` num SUPER_ADMIN real da instância — sem essas credenciais confirmadas e sem certeza de que o banco conectado é descartável, a execução foi adiada em vez de arriscar popular um banco não-descartável.
- Decisão de arquitetura documentada, não implementada: recomendado manter o login de operador como fluxo próprio dentro de Caixa/Cozinha em vez de também aceitá-lo em `/login` (avaliação completa no relatório final desta task), preservando auditoria e expiração curta do `operadorToken`.
- `GET /api/auth/me` avaliado e recomendado para task futura (não implementado nesta task).

**TASK-117 (redesign do login e entrada padrão pela ativação de dispositivo)**:
- Rota raiz `/` passou a redirecionar (`<Navigate replace>`) para `/ativar-dispositivo` — antes renderizava uma `HomePage` estática sem link algum, órfã desde a centralização de login (TASK-115); removida por não ter mais nenhuma rota apontando para ela.
- `/login` continua acessível diretamente e `/admin/login` continua redirecionando para `/login` (compatibilidade preservada, nada removido).
- `LoginPage` ganhou um layout dividido em duas colunas (`AuthSplitLayout`/`LoginBrandPanel`/`FoodIcons`, novos em `components/auth/`) — painel institucional com marca "TotemFood", três SVGs decorativos animados (hambúrguer/batata/bebida, os primeiros SVGs do projeto) e lista de recursos reais; formulário com eyebrow/título/descrição novos, sem citar perfis técnicos (`SUPER_ADMIN` etc.) na UI. **Sem link "Esqueceu a senha?"** (funcionalidade não existe). Toda a lógica de autenticação (`login()`, `resolveHomeRoute`, proteção contra open redirect, `location.state.from`) permanece intocada — mudança puramente visual/de composição.
- Navegação cruzada nova: "Ativar um dispositivo" em `/login` → `/ativar-dispositivo`; "Entrar como usuário" em `/ativar-dispositivo` → `/login`.
- Novos tokens de tema `--color-auth-brand-*` (`themes.css`) — painel institucional sempre escuro nos dois temas (identidade de marca fixa, não segue o tema geral do app); ver `docs/design-system/cores.md`.
- Animação dos ícones usa só `transform`/`opacity`, com keyframes que sempre voltam ao estado neutro — herda a regra global de `prefers-reduced-motion` já existente (`global.css`) sem nenhuma lógica JS nova.
- `npm test` → **149/149** (24 arquivos, 7 testes novos/ampliados: `AppRoutes.test.tsx` novo + `LoginPage.test.tsx` ampliado). `tsc -b` e `npm run build` → sem erro. `npx oxlint .` → exit 0, mesmos 2 warnings pré-existentes de `only-export-components`, nenhum novo. `npx playwright test` (suíte mockada) → **39/39** (9 novos em `e2e/redesign-login.spec.ts`; `admin-login.spec.ts` ajustado para o novo heading "Acesse sua conta").
- Validação visual manual (capturas Playwright locais, não commitadas como teste): dark e light desktop (1440px) e mobile (375px) conferidos — layout bate com a referência fornecida na task, especialmente no tema light (painel institucional escuro + formulário claro). Ajuste feito durante a validação: o painel do formulário passou a usar `--color-surface` em vez de `--color-bg` porque, no tema dark, os dois painéis ficavam quase indistinguíveis (ambos próximos de preto puro) até essa correção.
- `E2E integrado` não executado — mesma pendência de credenciais da TASK-116; esta task não toca nenhum contrato de backend, então o risco de regressão ali é nulo.

## Pendências

**TASK-088 (refresh de dispositivos e regeneração de ativação)**:
- Dispositivos agora recebem refresh token na ativação; `/api/auth/refresh` aceita titulares `Usuario` e `Dispositivo`, com rotação de uso único e um token ativo por titular.
- `api.ts` renova sessões de dispositivo após `401`, preservando o tipo de sessão no `localStorage`.
- `PATCH /api/admin/dispositivos/{id}/regenerar-codigo` respeita escopo `SUPER_ADMIN`/`ADMIN_RESTAURANTE`, gera código seguro e revoga refresh tokens anteriores sem invalidar access tokens JWT já emitidos.
- Testes de serviço e integração cobrem ativação, rotação, reutilização rejeitada, isolamento entre usuário/dispositivo e escopo administrativo.

**TASK-089 (validação real do refresh de dispositivos)**:
- Validado via `curl` contra backend real (equivalente funcional ao clique no navegador — sem automação disponível neste ambiente) para os três tipos de dispositivo: ativação retorna `accessToken`+`refreshToken`; `401` com token inválido dispara renovação via `/api/auth/refresh`; novo par de tokens permite repetir a chamada original com sucesso; refresh antigo (uso único) falha após rotação; refresh totalmente inválido falha sem loop.
- Regeneração de código validada para `SUPER_ADMIN` (qualquer restaurante) e `ADMIN_RESTAURANTE` (só o próprio restaurante, `403` para outro, `401` sem token); confirmado que o `refreshToken` anterior é revogado enquanto o `accessToken` JWT antigo segue válido até expirar (limitação stateless).
- `mvn test` → 240/240, BUILD SUCCESS; `npm run build` sem erro TypeScript; `npx oxlint` só o warning pré-existente. **Nenhum bug encontrado — nenhuma alteração de código nesta task.**
- Ver `docs/checklists/fluxo-operacional-mvp.md` seção 9 e `docs/checklists/admin-mvp.md` seção 13 para o detalhamento completo.

**TASK-090 (gestão de usuários pelo ADMIN_RESTAURANTE)**:
- `/api/admin/usuarios` deixou de ser exclusivo de `SUPER_ADMIN`: `ADMIN_RESTAURANTE` agora lista/cria/edita/ativa/desativa/altera senha de usuários `OPERADOR_CAIXA`/`OPERADOR_COZINHA` do próprio restaurante — nunca `SUPER_ADMIN`, nunca outro `ADMIN_RESTAURANTE`, nunca usuário/restaurante alheio. `SUPER_ADMIN` mantém acesso irrestrito, comportamento inalterado.
- `UsuarioService` ganhou `AdminScopeService` (mesmo padrão de Categoria/Produto/Dispositivo/Pedido/Dashboard desde a TASK-058) — antes desta task era o único módulo administrativo sem essa integração, o que teria permitido escalada de privilégio total se o controller fosse aberto sem isso (diagnosticado na TASK-090-A).
- Frontend: card "Usuários" visível para `SUPER_ADMIN` e `ADMIN_RESTAURANTE` (só oculto para `OPERADOR_CAIXA`/`OPERADOR_COZINHA`); `/admin/usuarios` trava restaurante e restringe perfis para `ADMIN_RESTAURANTE`, mesmo padrão de Dispositivos (TASK-059).
- **Importante, documentado explicitamente**: esta task só habilita o CRUD de `OPERADOR_CAIXA`/`OPERADOR_COZINHA` pelo Admin. Esses perfis continuam **sem nenhum acesso operacional real** — `/api/caixa/**`/`/api/cozinha/**` seguem exclusivamente autenticados por dispositivo. Login de operador dentro do dispositivo e auditoria por usuário humano (`HistoricoStatusPedido.alteradoPorUsuario`) ficam para task futura — ver Próximas tasks recomendadas.
- `mvn test` → 279/279, BUILD SUCCESS (inclui ajuste em `SecurityHttpStatusTest`, que usava `/api/admin/usuarios` como exemplo de 403 para `ADMIN_RESTAURANTE` — trocado para `/api/admin/restaurantes`, ainda exclusivo de `SUPER_ADMIN`); `npm run build`/`npx oxlint` sem erro. Ver `docs/checklists/admin-mvp.md` seção 14 e `docs/testes-backend-mvp.md` seção 7-octies.

**TASK-091 (decisão arquitetural: dispositivo x operador humano)**: análise arquitetural, sem código. Confirmou que o modelo implementado é o Modelo A (só dispositivo) e recomendou o Modelo C (dispositivo + operador humano opcional, aditivo) como caminho para a TASK-092 — ver relatório da própria task para o detalhamento completo (modelos comparados, riscos, MVP incremental sugerido).

**TASK-092 (login operacional de operador — Modelo C)**:
- Novo endpoint `POST /api/auth/operador/login`: exige dispositivo CAIXA/COZINHA autenticado, aceita `OPERADOR_CAIXA`/`OPERADOR_COZINHA`/`ADMIN_RESTAURANTE` compatíveis com o tipo do dispositivo e do mesmo restaurante; `SUPER_ADMIN` sempre `403`; dispositivo TOTEM/ADMINISTRACAO `403` automático. Retorna `operadorToken` curto (30min, sem refresh).
- `OperadorContextService` resolve o novo header opcional `X-Operador-Token` nas 5 ações de Caixa/Cozinha (confirmar pagamento, enviar à cozinha, retirar, cancelar, atualizar status) — revalida perfil/restaurante contra o dispositivo da requisição atual, nunca confia só no claim do token. Sem o header, comportamento idêntico ao anterior.
- `HistoricoStatusPedido.alteradoPorUsuario` (coluna já existente, nunca preenchida antes) passa a ser preenchido quando um operador válido é identificado, junto com `alteradoPorDispositivo` (já preenchido desde sempre) — sem migration.
- Frontend: `tokenStorage` ganhou storage separado (`totem.operadorToken`/`totem.operador`, nunca reaproveitando as chaves de dispositivo/usuário); novo componente `OperadorPainel` (identificação/troca de operador) em `/caixa` e `/cozinha`; `api.ts` anexa `X-Operador-Token` automaticamente quando há sessão de operador salva.
- **Importante, documentado explicitamente**: `/api/caixa/**`/`/api/cozinha/**` continuam exigindo exclusivamente o token de dispositivo — o operador é auditoria complementar opcional, nunca autenticação. PIN de operador e refresh token de operador ficam fora do escopo (email/senha + token curto sem refresh, decisão da TASK-092).
- `mvn test` → **320/320, BUILD SUCCESS**; `npm run build`/`npx oxlint` sem erro. Ver `docs/checklists/fluxo-operacional-mvp.md` seção 10 e `docs/testes-backend-mvp.md` seção 7-nonies.

**TASK-093 (validação funcional da auditoria de operador)**:
- Revalidação da TASK-092 via `curl` contra backend real (não só a suíte automatizada): login operacional em todas as combinações válidas (200) e todos os bloqueios (403/401); fluxo Totem→Caixa→Cozinha→Caixa completo sem operador (histórico só com dispositivo) e com operador (histórico com dispositivo **e** operador, nos dois módulos); troca de operador no mesmo dispositivo registrando corretamente cada um; token de operador inválido/incompatível sem afetar a sessão do dispositivo.
- **Bug real encontrado e corrigido**: `SecurityConfig.corsConfigurationSource()` não incluía `X-Operador-Token` em `allowedHeaders` — o preflight CORS do navegador teria bloqueado toda ação de Caixa/Cozinha com operador identificado (reproduzido via `curl` simulando o preflight antes da correção). Corrigido; `mvn test` → 320/320 depois da correção, sem regressão.
- `npm run build`/`npx oxlint` sem erro (nenhuma alteração de frontend nesta task — o bug era só de backend). Ver `docs/testes-backend-mvp.md` seção 7-decies e `docs/checklists/fluxo-operacional-mvp.md` seção 10.

**TASK-094 (validação operacional completa — cobertura estendida)**:
- Ampliou a revalidação da TASK-093 via `curl` contra backend real (mesma limitação de ambiente: sem automação de navegador disponível, equivalente funcional acordado com o solicitante): confirmou a correção do CORS ainda presente e efetiva; `ADMIN_RESTAURANTE` operando com sucesso tanto no Caixa quanto na Cozinha; toda a matriz de perfis incompatíveis (`OPERADOR_CAIXA`→Cozinha, `OPERADOR_COZINHA`→Caixa, `SUPER_ADMIN`, operador de outro restaurante, dispositivo TOTEM) rejeitada com `403`; senha errada e usuário inativo ambos com `401` e mensagem genérica idêntica (sem vazar qual condição falhou); troca de operador no mesmo dispositivo entre duas ações consecutivas do mesmo pedido, histórico atribuindo corretamente cada ação ao operador certo; token de operador de perfil incompatível usado deliberadamente no dispositivo/ação errado → `403`, sem alterar o pedido.
- Diferenciação operador × dispositivo confirmada por revisão de código (`CaixaHomePage.tsx`/`CozinhaHomePage.tsx`/`api.ts`): um `401` numa ação com operador identificado limpa **só** a sessão do operador (`clearOperadorSession`), preservando a sessão do dispositivo; um `401` por sessão de dispositivo genuinamente inválida (refresh token também rejeitado) aciona `clearSession()` completo (dispositivo + usuário + operador) dentro do próprio `api.ts`, antes mesmo de a tela tratar o erro — os dois caminhos são código já existente desde a TASK-092/093, não alterado nesta task.
- Expiração de token de operador validada por revisão de código (`JwtService.isTokenValido`, `catch (Exception ex)` genérico ao redor do parse) em vez de aguardar os 30 minutos reais ou alterar a configuração de expiração: o mesmo caminho que já retorna `401` para assinatura inválida (testado empiricamente) trata `ExpiredJwtException` de forma idêntica.
- **Nenhum bug adicional encontrado. Nenhuma alteração de código nesta task** (só documentação) — `git status` confirmou que o único arquivo de código modificado no repositório continua sendo `SecurityConfig.java` da correção da TASK-093, sem mudanças novas. Ver `docs/testes-backend-mvp.md` seção 7-undecies e `docs/checklists/fluxo-operacional-mvp.md` seção 10.

**TASK-095 (revisão de roadmap pós-MVP)**: análise e priorização, sem código — criou `docs/roadmap-pos-mvp.md` (P0/P1/P2/P3, sequência de 10 próximas tasks). Reclassificou como **P0** três itens que antes estavam listados só como "melhoria": seed de `SUPER_ADMIN` com senha fixa pública, `JWT_SECRET` com fallback silencioso de desenvolvimento, e CORS com origens hardcoded em `localhost` — os três bloqueiam qualquer deploy fora do laptop de desenvolvimento ou expõem credencial/segredo publicamente. Ver `docs/roadmap-pos-mvp.md` para o detalhamento completo.

**TASK-096 (seed de SUPER_ADMIN seguro)**: corrige o primeiro P0 da TASK-095. Nova migration `V7` desativa o `SUPER_ADMIN` de seed (`admin@totem.local`) em qualquer instalação onde a senha (`Admin@2026!`, antes documentada em texto claro) nunca tenha sido trocada — `V4`/`V5` não foram editadas, para não quebrar o checksum do Flyway em bancos onde já foram aplicadas. Novo `SuperAdminBootstrapRunner` (`ApplicationRunner` condicional a `app.bootstrap.super-admin.enabled`, default `false`) passa a ser o único caminho para criar o primeiro `SUPER_ADMIN` de um ambiente, exigindo `SUPER_ADMIN_EMAIL`/`SUPER_ADMIN_PASSWORD` explícitos (sem eles, falha o startup em vez de criar algo silencioso ou usar um valor padrão). `mvn test` → **324/324, BUILD SUCCESS** (320 + 4 novos testes do bootstrap). Nenhuma alteração de frontend. Ver `docs/testes-backend-mvp.md` seção 7-tredecies e `docs/04-seguranca.md`.

**TASK-097 (JWT secret sem fallback inseguro)**: corrige o segundo P0 da TASK-095. `application.yml` tinha `secret: ${JWT_SECRET:uma-chave-local-de-desenvolvimento-...}` — um fallback fixo e publicamente conhecido, usado silenciosamente para assinar tokens de USER/DEVICE/OPERADOR (mesma chave para os três) se `JWT_SECRET` não fosse definido. Removido o fallback (`secret: ${JWT_SECRET:}`); novo `JwtSecretValidator` roda no construtor de `JwtService` e falha o startup com mensagem clara se o secret estiver ausente, com menos de 32 caracteres, ou igual ao valor antigo conhecido. `backend/src/test/resources/application.yml` já tinha um secret próprio isolado e adequado — nenhum ajuste necessário lá. Decisão consciente de **não** criar um profile `dev` com secret próprio: `JWT_SECRET` passa a ser obrigatório inclusive em desenvolvimento local, para não reabrir a mesma classe de risco por outro caminho. `mvn test` → **330/330, BUILD SUCCESS** (324 + 6 novos testes do validador). Nenhuma alteração de frontend, CORS, seed de SUPER_ADMIN, claims ou expiração. Ver `docs/testes-backend-mvp.md` seção 7-quattuordecies e `docs/04-seguranca.md`.

**TASK-098 (CORS externalizado por ambiente)**: corrige o terceiro e último P0 da leva de hardening da TASK-095. `SecurityConfig.corsConfigurationSource()` tinha as origens hardcoded (`http://localhost:5173`/`5174`) — funcionava só em dev local, qualquer domínio de produção exigiria alterar código. Origens agora vêm de `app.security.cors.allowed-origins` (`CORS_ALLOWED_ORIGINS`, lista por vírgula), validadas por novo `CorsOriginsValidator` (falha o startup se ausente, contendo `*`, ou com origem sem protocolo explícito). Métodos e headers permitidos (incluindo `X-Operador-Token`) não mudaram. `backend/src/test/resources/application.yml` ganhou a mesma configuração de dev, já que o bean de CORS (diferente do bootstrap de SUPER_ADMIN) é sempre instanciado — as 13 classes `@SpringBootTest` do projeto dependiam disso. Novo `CorsConfigurationIntegrationTest` (MockMvc) é a primeira cobertura automatizada de CORS do projeto: confirma preflight com origem permitida, `X-Operador-Token` aceito, e origem não configurada sem `Access-Control-Allow-Origin`. `mvn test` → **341/341, BUILD SUCCESS** (330 + 11 novos: 8 do validador, 3 de integração). Nenhuma alteração de frontend, JWT, seed de SUPER_ADMIN ou autenticação. Ver `docs/testes-backend-mvp.md` seção 7-quindecies e `docs/04-seguranca.md`.

**TASK-099 (observabilidade mínima — Actuator + logs operacionais)**: primeiro item P1 do roadmap pós-MVP, executado logo após a leva de hardening P0. Adiciona `spring-boot-starter-actuator` com `management.endpoints.web.exposure.include: health,info` — só `/actuator/health` e `/actuator/info` ficam públicos (liberados em `SecurityConfig`); qualquer outro endpoint do Actuator não tem exposição habilitada e nem existe como rota. `/api/health` legado não foi alterado, continua público por compatibilidade. Duas lacunas de log fechadas: `CozinhaPedidoService.atualizarStatus` ganhou log (não tinha nenhum); `SuperAdminBootstrapRunner` deixou de ser `@ConditionalOnProperty` (o bean não existia quando desabilitado, então esse caso nunca tinha como logar) e passou a checar `enabled` internamente, logando os dois estados — comportamento funcional idêntico. Novo `ActuatorSecurityIntegrationTest` (5 casos) confirma que `/actuator/env` nunca retorna `200` e que endpoints protegidos continuam exigindo token. `mvn test` → **347/347, BUILD SUCCESS** (341 + 6 novos). Nenhuma alteração de frontend, autenticação ou regra de negócio. Ver `docs/testes-backend-mvp.md` seção 7-sedecies e `docs/04-seguranca.md`.

**TASK-099.1 (correção de CI — falha nos jobs backend do GitHub Actions)**: *(renumerada de "TASK-100" nesta consolidação — o roadmap já reservava TASK-100 para "Branch protection + badge de CI" desde a TASK-095; ver TASK-100 abaixo)*.
- **Bug real encontrado, não relacionado à hipótese inicial**: `UploadImagemServiceTest.falhaAoExcluirUmArquivoNaoInterrompeOsDemais` simulava falha de exclusão com `arquivo.toFile().setWritable(false)`. No Windows isso bloqueia `Files.delete`, mas no Linux (runners `ubuntu-latest` do GitHub Actions) a permissão de excluir um arquivo é do diretório pai, não do arquivo — o bit somente-leitura não impede o delete, então a asserção de falha nunca era satisfeita, quebrando o teste só no CI. Confirmado reproduzindo o comportamento num container `eclipse-temurin:21-jdk` local (Docker).
- Faltavam variáveis `JWT_SECRET`/`CORS_ALLOWED_ORIGINS` no `ci.yml` (hipótese inicial da investigação), mas isso **não** era a causa: `backend/src/test/resources/application.yml` já fornece esses valores como literais (não como fallback de env var), então tanto `mvn test` quanto `mvn verify -Ppostgres-it` são independentes de env vars do CI. `ci.yml` não foi alterado.
- Corrigido `UploadImagemServiceTest` para simular a falha via `Mockito.mockStatic(Files.class)` (interceptando só o `Files.delete` do arquivo-alvo, com `CALLS_REAL_METHODS` para o resto), eliminando a dependência de semântica de permissão de SO.
- Validado: `mvn test` completo → **347/347, BUILD SUCCESS** no Windows; repetido dentro de container Linux (`eclipse-temurin:21-jdk` + Maven 3.9.9, mesmo `~/.m2` local) → **347/347, BUILD SUCCESS**, confirmando paridade com o runner do CI. Nenhuma alteração de frontend. Segurança das TASK-096/097/098 preservada — nenhuma alteração em `JwtSecretValidator`, `CorsOriginsValidator`, `SecurityConfig` ou `application.yml`.

**TASK-100.1 (limpeza de artefatos de build rastreados pelo Git)**: não havia `.gitignore` na raiz do repositório nem em `backend/` (só `frontend/` tinha um) — `backend/target/` (326 arquivos: `.class` compilados, relatórios do Surefire, `.lst` do maven-status, recursos copiados) estava versionado, junto com dois logs de execução local (`backend/backend_run.log`, `backend/backend_run_diag.log`). Criado `.gitignore` na raiz cobrindo `target/`, `**/target/`, `build/`, `dist/`, `*.log`, `node_modules/`, `.env` (exceto `.env.example`) e itens de IDE/SO; artefatos removidos do índice via `git rm -r --cached` (arquivos mantidos localmente). Confirmado via `git ls-files backend/target` (vazio) e `git check-ignore -v` que o padrão cobre o caminho. Nenhum código-fonte, migration, doc, `pom.xml` ou `package-lock.json` foi tocado.

**TASK-100 (branch protection + badge de CI)**: primeiro item P1 da tabela de priorização do roadmap pós-MVP (pendente desde a TASK-084). Badge do GitHub Actions adicionado no topo do `README.md`, apontando para `.github/workflows/ci.yml` (owner/repo confirmados via `git remote -v`: `FabioSimones/TOTEM-2026`). Criado `docs/ci-branch-protection.md` com o checklist manual de configuração (Settings → Branches → Branch protection rules), os três checks obrigatórios (`Backend (H2)`, `Backend (PostgreSQL/Testcontainers)`, `Frontend (build + lint)`), política de merge recomendada, procedimento para investigar falha de CI e a relação com o `.gitignore` da TASK-100.1. **Documentação concluída; configuração de branch protection no GitHub em si continua sendo um passo manual pendente** (exige acesso administrativo ao repositório, não é feita via API/código por esta task — fora de escopo, conforme definido). Nenhuma alteração de código de produção, teste ou workflow.

**TASK-101 (testes frontend automatizados mínimos com Vitest + Testing Library)**: primeiro item de teste automatizado do roadmap pós-MVP. Adicionadas devDependencies `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`; `vite.config.ts` ganhou bloco `test` (`environment: "jsdom"`, `setupFiles: "./src/test/setup.ts"`, `globals: true`); novo `src/test/setup.ts` carrega os matchers do `jest-dom` e fixa `process.env.TZ = "UTC"` para os testes de data/hora serem determinísticos; `tsconfig.app.json` ganhou `"vitest/globals"`, `"@testing-library/jest-dom"` e `"node"` em `types` (necessário para `tsc -b`, usado pelo `npm run build`, tipar os arquivos de teste sem erro). Scripts novos: `npm test` (`vitest run`, usado no CI) e `npm run test:watch`. Convenção adotada: arquivo `*.test.ts(x)` ao lado do código testado.

37 testes cobrindo: `tokenStorage.ts` (sessão de usuário/dispositivo/operador, incluindo dois comportamentos reais não óbvios encontrados na leitura do código — `saveUserSession` não limpa operador, `saveDeviceSession` limpa usuário **e** operador — testados como estão implementados, não como uma suposição inicial assumia); `dateTime.ts` (`parseBackendUtcDateTime` via `toISOString()`, determinístico independente de fuso; `formatarDataReferencia` puro, sem `Date`); `adminScope.ts` (reconhecimento de perfil e escopo por restaurante); `OperadorPainel.tsx` (os dois estados do componente, submissão do formulário com `authService.loginOperador` mockado, "Trocar operador" usando `tokenStorage` real); `api.ts` (renovação automática de sessão em `401` com `fetch` mockado via `vi.stubGlobal` — `403` não tenta renovar, `401` sem `refreshToken` limpa a sessão sem chamar o backend).

**Não implementado, por não existir hoje em `api.ts`**: a regra "401 com `X-Operador-Token` inválido limpa só o operador" mencionada no briefing da task não tem código correspondente (a única lógica de `401` existente é a renovação de `accessToken`/`refreshToken`) — não foi testada para não inventar comportamento que a implementação não tem.

`.github/workflows/ci.yml` ganhou o passo `npm test` no job `frontend`, antes de `npm run build`/`npm run lint` — jobs de backend não foram tocados. Validado: `npm test` → **37/37 passed**; `npm run build` → sem erro TypeScript; `npm run lint`/`npx oxlint` → só o warning cosmético pré-existente (`ThemeContext.tsx`, `react/only-export-components`). Nenhuma alteração de backend, regra de negócio ou componente de produção — só os arquivos de teste/config listados acima. Sem Playwright/Cypress, sem E2E, sem cobertura de `CaixaHomePage`/`CozinhaHomePage`/telas Admin nesta task (fora de escopo).

**TASK-102 (homologação visual real com Playwright headless)**: fecha a pendência histórica de "clique real" em Totem/Caixa/Cozinha/operador (TASK-094.1), bloqueada em quatro tentativas anteriores por ausência de ferramenta de automação de navegador. Adicionada devDependency `@playwright/test`; browser Chromium instalado via `npx playwright install --with-deps chromium` (não versionado). Novo `frontend/playwright.config.ts` (`testDir: "./e2e"`, `baseURL`/`webServer` em `http://127.0.0.1:5173` — host consistente entre os dois, já que `localStorage` é isolado por origem e um mismatch `localhost`/`127.0.0.1` faria a sessão injetada não aparecer para a página). Scripts novos: `npm run e2e`, `e2e:ui`, `e2e:headed`, `e2e:report`. Estratégia adotada (conforme recomendação do briefing): API 100% mockada via `page.route` (`e2e/helpers/mockApi.ts`), sessão de dispositivo injetada direto no `localStorage` antes do carregamento (`e2e/helpers/storage.ts`, `page.addInitScript`) — nenhum teste depende de backend real, banco de dados ou rede. `.gitignore` ganhou `test-results/`, `playwright-report/`, `blob-report/`.

7 testes cobrindo: `e2e/admin-login.spec.ts` (login real com sucesso e com credenciais inválidas, navegação para `/admin`, `localStorage` real); `e2e/operador-painel.spec.ts` (`OperadorPainel` dentro do Caixa — formulário, submissão real, troca visual para operador identificado, "Trocar operador" volta ao formulário — regra crítica da TASK-092, agora com clique real automatizado); `e2e/fluxo-operacional-mock.spec.ts` (Totem carrega cardápio e "Adicionar" habilita o resumo do pedido; Caixa carrega a lista e mostra o botão de ação sugerida; Cozinha carrega a lista e mostra o botão de avanço de status — cada terminal testado isoladamente, sem encadear um pedido real entre os três, conforme escopo definido).

**Bug real de produção encontrado por esta suíte**: o teste do Totem falhava de forma determinística (produto reportado como "hidden" pelo Playwright, apesar de presente na árvore de acessibilidade). Investigação via estilos computados (`page.evaluate`) revelou que, em `frontend/src/styles/global.css`, a regra base `.cart-summary { width: 100%; }` vinha **depois**, no arquivo, do bloco `@media (min-width: 960px) { .cart-summary { width: 22rem; } }` — com especificidade igual, a ordem no arquivo decide a cascata, então a regra base sobrescrevia silenciosamente a de desktop. Impacto real (não só do teste): em qualquer tela ≥960px, o carrinho do `/totem` ocupava 100% da largura e o grid de produtos colapsava para ~0px, visualmente quebrado — bug presente em produção desde que esse CSS foi escrito, nunca detectado por `npm run build`/`oxlint`/revisão de código, só por clique real. Corrigido reordenando o CSS (mobile-first: regra base antes do `@media`), sem alterar nenhum valor. Este é o único ponto de `backend`/`frontend` de produção alterado nesta task — justificado pela regra "bug real mínimo indispensável" do escopo.

Ao rodar a suíte de Vitest logo após instalar o Playwright, `npm test` também tentou executar os specs do Playwright (padrão default do Vitest casa `**/*.spec.ts`, incluindo `e2e/`), colidindo `test`/`describe` das duas bibliotecas — corrigido restringindo `vite.config.ts` (`test.include: ['src/**/*.test.{ts,tsx}']`), formalizando a convenção já em uso (Vitest = `*.test.ts(x)` em `src/`, Playwright = `*.spec.ts` em `e2e/`).

**CI**: Playwright **não** foi adicionado ao `.github/workflows/ci.yml` nesta task — decisão deliberada (primeira introdução de E2E, estabilizar local antes; instalar browser no CI é custo adicional a avaliar numa task própria). CI atual (`npm test`/`npm run build`/`npm run lint`) preservado sem alteração. Validado: `npm test` → 37/37 (sem regressão); `npm run build` → sem erro TypeScript; `npm run e2e` → **7/7 passed**; `npm run lint` → só o warning cosmético pré-existente. Nenhuma alteração de backend.

**TASK-103 (Playwright no CI)**: fecha a pendência deixada em aberto pela TASK-102. Novo job `frontend-e2e` (nome exibido `Frontend E2E (Playwright)`) em `.github/workflows/ci.yml`, com `needs: frontend` — só roda depois que `npm test`/`npm run build`/`npm run lint` (job `frontend`) já passaram, evitando gastar tempo de runner instalando browser se o básico do frontend já estiver quebrado. Passos: checkout → `setup-node` (Node 20, mesmo padrão do job `frontend`) → `npm ci` → `npx playwright install --with-deps chromium` (só Chromium, não a suíte completa de browsers) → `npm run e2e`. Em falha, `actions/upload-artifact@v4` publica `frontend/playwright-report/` e `frontend/test-results/` (nome do artifact `playwright-report`, retenção de 7 dias, `if: failure()` — nada é publicado em sucesso). Não sobe nenhum backend: a suíte inteira já era mockada via `page.route` desde a TASK-102, então o job de E2E não tem `needs` nos jobs de backend. Nenhum spec E2E foi alterado (nenhum bug de CI encontrado que exigisse isso). Validado: `npm test` → 37/37; `npm run build` → sem erro; `npm run e2e` → **7/7 passed** (local, mesmo comando do CI); `npm run lint` → só o warning cosmético pré-existente. Nenhuma alteração de backend, de spec E2E ou de regra de negócio — só `.github/workflows/ci.yml` e documentação. **CI do GitHub Actions em si não foi validado nesta execução** (sem push/PR real disparado durante a task) — próximo push/PR deve confirmar os 4 jobs verdes.

**TASK-103.1 (validação do primeiro run real do Playwright no GitHub Actions)**: **Cenário A — tudo verde**, sem necessidade de correção. Run `29361958515` (commit `0f98475`, `push` para `main`, 2026-07-14T19:28Z), consultado via API pública do GitHub (`GET /repos/FabioSimones/TOTEM-2026/actions/runs`) já que `gh` CLI não está disponível neste ambiente: os 4 jobs completaram com `conclusion: success`. `Frontend E2E (Playwright)`: `npm ci` → `npx playwright install --with-deps chromium` → `npm run e2e`, todos `success`; step de upload de artifact corretamente `skipped` (roda só em falha); `GET /actions/runs/{id}/artifacts` confirmou `total_count: 0`. Job levou ~40s; workflow completo (4 jobs) ~3min. Nenhuma alteração de código foi necessária — nem `ci.yml`, nem `playwright.config.ts`, nem nenhum spec E2E. **Recomendação**: manter `Frontend E2E (Playwright)` como check recomendado (não obrigatório) em `docs/ci-branch-protection.md` por mais 1-2 runs, para descartar flakiness específica do runner `ubuntu-latest` antes de bloquear PRs legítimos por ele; este foi o primeiro run real observado.

**TASK-104 (E2E integrado com backend real)**: primeira suíte E2E sem mocks, provando que o frontend real conversa com um backend Spring Boot + PostgreSQL reais — até aqui, toda a homologação visual automatizada (TASK-102/103) usava `page.route`. Novos: `frontend/playwright.integrado.config.ts` (testDir `./e2e-integrado`, porta `127.0.0.1:5174` — diferente da mockada, para não colidir se rodarem em sequência; `webServer` sobe só o Vite, nunca o backend; `globalSetup` checa `/actuator/health` antes de qualquer teste, falhando rápido e com mensagem clara se o backend não estiver de pé); `frontend/e2e-integrado/helpers/backendApi.ts` (prepara dados via chamadas HTTP reais — login SUPER_ADMIN, cria restaurante/categoria/produto/dispositivo, ativa o dispositivo — tudo com sufixo único por execução, `E2E_<timestamp>`, sem depender de limpeza de banco); `frontend/e2e-integrado/helpers/storage.ts` (injeta os tokens **reais** da ativação no `localStorage`, diferente do helper da suíte mockada que usa strings fictícias); `frontend/e2e-integrado/totem-pedido-real.spec.ts` (Fluxo A do briefing: Totem carrega cardápio real → adiciona produto → cria pedido real → paga com Pix → `AUTORIZADO`/`PAGO`).

Escolhas de escopo, conforme recomendação do briefing: só o Fluxo A (Totem) — Caixa/Cozinha/operador com backend real ficam para task futura; backend **não** é subido pelo Playwright (precisa já estar rodando, documentado); CNPJ único gerado a partir de `Date.now()` (só precisa satisfazer o regex de 14 dígitos do backend, sem checksum real exigido); dados de teste se acumulam no banco entre execuções (aceitável para banco descartável local).

**Validado de ponta a ponta com backend real de verdade** (não só localmente com mocks): subido PostgreSQL 16 via Docker (`docker run postgres:16`, porta `55432`) e o backend real (`mvn spring-boot:run`) com `JWT_SECRET`/`CORS_ALLOWED_ORIGINS=http://127.0.0.1:5174`/bootstrap de SUPER_ADMIN configurados; `npm run e2e:integrado` → **1/1 passed** já na primeira tentativa; persistência real confirmada à parte, consultando `GET /api/admin/restaurantes` com o token do SUPER_ADMIN de teste e encontrando o restaurante criado pelo teste (`Restaurante E2E_<timestamp>`) — prova de que não há mock nenhum no caminho. Backend e container Docker desligados/removidos ao final, sem recursos órfãos. `npm test` → 37/37; `npm run build` → sem erro; `npm run e2e` (suíte mockada) → 7/7, sem regressão; `npm run lint` → só o warning cosmético pré-existente.

**TASK-105 (E2E integrado Caixa/Cozinha/operador)**: fecha a lacuna deixada pela TASK-104 (só Totem). Ampliado `frontend/e2e-integrado/helpers/backendApi.ts` de forma aditiva (nenhuma função/assinatura existente foi alterada de forma incompatível — `criarDispositivo` ganhou um 6º parâmetro opcional `tipoDispositivo` com default `"TOTEM"`, preservando todas as chamadas antigas): `criarUsuarioOperador`, `criarPedidoTotem`/`pagarPedidoTotem` (criam e pagam um pedido real via API do Totem, sem UI), `consultarPedidoAdmin` (`GET /api/admin/pedidos/{id}`), e a orquestração `prepararCenarioCaixaCozinha` (restaurante+produto, dispositivos CAIXA e COZINHA ativados, dois usuários `OPERADOR_CAIXA`/`OPERADOR_COZINHA`, um pedido já `PAGO`). Novo spec `frontend/e2e-integrado/caixa-cozinha-operador-real.spec.ts`: dois `BrowserContext` do Playwright simulando dois terminais físicos ao mesmo tempo — Caixa identifica operador real e envia para cozinha; Cozinha identifica o outro operador real, avança `EM_PREPARO`→`PRONTO`; volta ao Caixa, atualiza a lista, marca `RETIRADO`; validação final via `GET /api/admin/pedidos/{id}` confirma `statusPedido=RETIRADO` e que o histórico de auditoria (`alteradoPorUsuarioNome`/`alteradoPorDispositivoNome`) atribui corretamente cada transição ao operador+dispositivo certo — a mesma regra crítica da TASK-092, agora validada de ponta a ponta contra backend real. Ações de UI escopadas ao card do pedido específico (`page.locator("article", { has: heading(numeroPedido) })`), não por posição na lista, porque os dados se acumulam no banco entre execuções (sem cleanup) e uma busca genérica correria o risco de acertar um pedido de execução anterior. Diálogos `window.confirm` aceitos automaticamente via `page.on("dialog", ...)`.

Pedido preparado via API (Totem), não via UI — decisão de escopo conforme o briefing: o foco desta task é Caixa/Cozinha/operador, o fluxo de criação de pedido pela UI do Totem já está coberto no spec da TASK-104, repeti-lo aqui só deixaria o teste mais lento sem ganho de cobertura.

**Validado de ponta a ponta com backend real**: mesmo setup da TASK-104 (PostgreSQL 16 via Docker + `mvn spring-boot:run` com as mesmas env vars); `npm run e2e:integrado` → **2/2 passed** (os dois specs, Totem + Caixa/Cozinha/operador, ~10s no total) já na primeira tentativa; confirmado de forma independente consultando `GET /api/admin/pedidos?statusPedido=RETIRADO` e encontrando o pedido do teste. Backend e container Docker desligados/removidos ao final. `npm test` → 37/37; `npm run build` → sem erro; `npm run e2e` (mockada) → 7/7, sem regressão; `npm run lint` → só o warning cosmético pré-existente. **Não colocado no CI** (mesma decisão da TASK-104, ainda não revisitada). Nenhuma alteração de backend, regra de negócio, autenticação, CORS ou JWT.

**CI**: `frontend-e2e-integrado` **não** foi adicionado ao `ci.yml` nesta task (decisão deliberada, conforme o briefing) — subir PostgreSQL + backend Spring Boot no runner é complexidade adicional (tempo de boot, gerenciar Testcontainers vs. serviço dedicado, segredos de CI) que merece task própria depois que esta primeira versão provar valor localmente. Nenhuma alteração de backend, regra de negócio, autenticação, CORS ou JWT.

**TASK-106 (automatizar E2E integrado no CI)**: fecha a pendência deixada pela TASK-104/105. Novo job `frontend-e2e-integrado` (nome exibido `Frontend E2E Integrado (Backend real)`) em `.github/workflows/ci.yml`, com `needs: [frontend, frontend-e2e, backend-postgres-it]` — só roda depois que o básico do frontend, o E2E mockado e a integração real do backend (Testcontainers) já passaram, já que é o job mais caro do workflow. Usa o recurso nativo `services:` do GitHub Actions para subir PostgreSQL 16 (efêmero, sem Docker Compose novo), sobe o backend real em background (`nohup mvn -B spring-boot:run > backend-e2e-integrado.log 2>&1 & disown`) com `JWT_SECRET`/`CORS_ALLOWED_ORIGINS`/bootstrap de SUPER_ADMIN definidos direto no `env:` do job (valores descartáveis, banco efêmero por run — não são segredos reais, sem necessidade de GitHub Secrets), aguarda `/actuator/health` responder (loop de até 120s, imprime as últimas 100 linhas do log e falha com mensagem clara se estourar o tempo), depois `npm ci` → instala só o Chromium → `npm run e2e:integrado`. Em falha, publica artifact `playwright-report-integrado` (relatório HTML + `test-results-integrado/` + o log do backend, para diferenciar falha de infraestrutura de falha de teste) — nada é publicado em sucesso.

**Validação da sintaxe YAML**: parseado com `js-yaml` via `npx` (sem erro) antes do push, já que não há `gh`/actionlint disponível neste ambiente para uma validação mais forte.

**Validado localmente reproduzindo o mesmo padrão do job antes do push** (não só confiando na leitura do YAML): subido PostgreSQL 16 via Docker na mesma configuração do `services:`, backend real iniciado com o exato comando `nohup mvn -B spring-boot:run & disown` usado no workflow, healthcheck confirmado em 3s, `npm run e2e:integrado` → **2/2 passed**. Backend e container Docker desligados/removidos ao final, sem recursos órfãos. `npm test` → 37/37; `npm run build` → sem erro; `npm run e2e` (mockada) → 7/7, sem regressão; `npm run lint` → só o warning cosmético pré-existente. **Job não foi adicionado a `docs/ci-branch-protection.md` como obrigatório** (decisão explícita do escopo desta task — precisa de vários runs reais no GitHub Actions primeiro, mais superfície de instabilidade transitória que o E2E mockado por subir banco+backend reais). Nenhuma alteração de backend, regra de negócio, autenticação, CORS ou JWT — só `.github/workflows/ci.yml` e documentação.

### Críticas (impedem uso do MVP)

Nenhuma identificada nesta consolidação.

### Importantes (devem entrar nas próximas tasks)

- ~~Sem teste HTTP de autorização para `/api/admin/uploads/**`~~ **fechada na TASK-082** — `UploadAdminIntegrationTest` (9 testes) cobre autorização, multipart real e acesso público.
- ~~Clique real em UI sem automação de navegador~~ **fechada em grande parte na TASK-086** — com o CORS corrigido (TASK-085), login SUPER_ADMIN, Admin Home, Dashboard, Pedidos, Dispositivos, Produtos, Categorias, Restaurantes, Usuários, login `ADMIN_RESTAURANTE` (escopo) e refresh token foram todos clicados de verdade no navegador, sem nenhum bug encontrado. A TASK-089 cobriu o refresh de dispositivo (Totem/Caixa/Cozinha) e a regeneração de código, mas só via `curl` (equivalente funcional) — ainda não coberto por clique real de fato: fluxo Totem/Caixa/Cozinha completo, rate limit (`429`) no navegador, consistência visual detalhada tela a tela. Segue sem automação repetível neste ambiente — cada rodada de validação depende de alguém disponível para testar manualmente.
- ~~Sem Testcontainers/PostgreSQL real automatizado~~ **fechada parcialmente na TASK-083** — `mvn verify -Ppostgres-it` cobre fuso horário e expiração de pedidos (os dois pontos onde bugs reais já apareceram) contra Postgres 16 real, migrations Flyway reais. Ainda não cobre o fluxo operacional completo nem os demais módulos administrativos contra Postgres real — ver Melhorias.
- ~~Sem testes frontend automatizados~~ **fechada em duas camadas**: unitária/componente na TASK-101 (Vitest + Testing Library, 37 testes); E2E/visual na TASK-102 (Playwright headless, 7 testes, mockado). Ainda sem E2E integrado com backend real nem cobertura do fluxo completo Totem→Caixa→Cozinha encadeado — ver Melhorias.

### Melhorias (podem ficar para depois)

- Dashboard "hoje" continua calculado em UTC, não em `America/Sao_Paulo` (limitação documentada desde a TASK-074, mantida deliberadamente).
- Contratos ainda usam `LocalDateTime` sem offset explícito — o frontend contorna isso desde a TASK-080 (`utils/dateTime.ts`), mas a migração para `Instant`/`OffsetDateTime` no backend continua sendo a solução mais robusta a longo prazo (fora de escopo de várias tasks já).
- `Maven` fora do `PATH` do shell padrão deste ambiente — precisa localizar via `~/.m2/wrapper/dists` a cada sessão nova (documentado em memória do agente).
- `oxlint`: warning de `react/only-export-components` em `ThemeContext.tsx` — cosmético, não afeta funcionamento.
- Sem WebSocket/heartbeat — status operacional de dispositivos e listas do Caixa/Cozinha dependem de atualização manual ou polling leve.
- Sem estorno de pagamento no cancelamento de pedido `PAGO` (decisão documentada desde a TASK-024).
- Sem Pix real/TEF/SmartPOS — só `FakePaymentProvider` simulado.
- Suíte Testcontainers (TASK-083) cobre só fuso horário/expiração — o fluxo operacional completo (Totem→Caixa→Cozinha) e os demais módulos administrativos continuam validados apenas contra H2 automatizado + Postgres manual.
- Suíte Vitest do frontend (TASK-101) cobre lógica pura e um componente isolado; a suíte Playwright (TASK-102, rodando no CI desde a TASK-103) cobre login Admin, `OperadorPainel` e uma fatia visual mínima de Totem/Caixa/Cozinha, toda mockada. A TASK-110 adicionou `Modal.test.tsx` (Vitest) e um teste de página representativo (`AdminRestaurantesPage.test.tsx`) para o novo padrão de modal — as demais 4 páginas administrativas (Dispositivos/Categorias/Produtos/Usuários) seguem o mesmo código mas não ganharam teste próprio ainda. A TASK-111 adicionou `CaixaHomePage.test.tsx`/`CozinhaHomePage.test.tsx` (gate de operador) e atualizou `fluxo-operacional-mock.spec.ts` para logar operador antes de checar a lista. A TASK-115 adicionou `FieldError.test.tsx`/`Input.test.tsx`, testes de componente para `RestauranteForm`/`ProdutoForm`/`UsuarioForm` (validação inline) e ampliou `AdminRestaurantesPage.test.tsx` com os casos "não fecha o modal em erro"/"erro de campo da API"/"sem erro residual ao reabrir" — `DispositivoForm`/`CategoriaForm` e as páginas de Dispositivos/Categorias/Produtos/Usuários seguem o mesmo padrão de validação mas ainda sem teste de componente/página próprio. Ainda faltam: E2E integrado com backend real (Testcontainers por trás do frontend), fluxo operacional completo encadeado (um pedido real passando pelos três terminais), e cobertura Playwright das telas Admin de CRUD (restaurantes/categorias/produtos/usuários/dispositivos) além do fluxo de validação inline (TASK-115) — validado manualmente por captura de tela na TASK-110 (login centralizado e modal de restaurante em desktop/mobile), mas sem spec Playwright permanente para o CRUD completo.
- ~~`mvn verify -Ppostgres-it` exige Docker disponível — não roda em CI/ambiente sem Docker; nenhum pipeline CI foi criado nesta task~~ **fechada na TASK-084** — `.github/workflows/ci.yml` roda `backend-h2`, `backend-postgres-it` (Docker nativo em runners `ubuntu-latest`) e `frontend` em jobs paralelos, em `pull_request` e `push` para `main`.

## Próximas tasks recomendadas

1. ~~TASK-094.1 — Homologação visual do fluxo operacional em navegador real~~ **fechada de forma automatizada pelas TASK-102 (mockado), TASK-104 (real, Totem) e TASK-105 (real, Caixa/Cozinha/operador)**: Playwright headless agora clica de verdade em login Admin, `OperadorPainel` (identificar/trocar operador), carregamento visual de Totem/Caixa/Cozinha (TASK-102, mockado), o fluxo completo do Totem contra backend real (TASK-104), e o fluxo completo Caixa→Cozinha→Caixa com operadores reais e histórico de auditoria validado contra backend real (TASK-105) — a pendência histórica de "clique real" (desde a TASK-060) tem cobertura automatizada mockada e integrada nos três terminais operacionais. **Ainda em aberto**: um fluxo Totem→Caixa→Cozinha encadeado de ponta a ponta com o **mesmo** pedido passando pelos três terminais na mesma execução (a TASK-105 cria o pedido via API, não pela UI do Totem, para focar em Caixa/Cozinha) — isso continua exigindo validação manual (`curl`, como nas TASK-089/092/093/094) ou uma task futura encadeando os dois specs integrados.
2. ~~Considerar badge de status do CI no `README.md` e, se o time adotar branch protection no GitHub, exigir os 3 jobs de `ci.yml` como check obrigatório antes de merge em `main`~~ **documentação concluída na TASK-100** — badge adicionado ao `README.md`, checklist completo de configuração em `docs/ci-branch-protection.md`. A configuração da regra em si no GitHub (Settings → Branches) continua sendo um passo manual pendente, que exige acesso administrativo ao repositório.
3. ~~Configurar Vitest + Testing Library no frontend~~ **concluída na TASK-101**; ~~Homologação visual real (Playwright headless)~~ **concluída na TASK-102** — ver detalhamento acima e `frontend/README.md` seções "Testes automatizados" e "Testes E2E (Playwright)".
4. ~~Avaliar incluir Playwright no `.github/workflows/ci.yml`~~ **concluída na TASK-103** — job `Frontend E2E (Playwright)` rodando com `needs: frontend`, artifact de relatório publicado só em falha, primeiro run real validado na TASK-103.1. ~~E2E integrado com backend real~~ **Totem concluído na TASK-104, Caixa/Cozinha/operador concluído na TASK-105**. ~~Avaliar colocar `e2e-integrado` no CI~~ **concluída na TASK-106** — job `Frontend E2E Integrado (Backend real)` sobe PostgreSQL+backend reais no próprio runner, validado localmente antes do push, ainda não é check obrigatório (precisa de vários runs reais primeiro). **Próxima recomendada**: acompanhar os primeiros runs reais deste job no GitHub Actions (equivalente à TASK-103.1, mas para o job integrado) e, se estável, considerar promovê-lo; alternativa/complementar: encadear um fluxo Totem→Caixa→Cozinha com o mesmo pedido na mesma execução.
5. ~~Decisão arquitetural: operador humano x dispositivo no fluxo Caixa/Cozinha~~ **decidida na TASK-091 (Modelo C) e implementada na TASK-092** — login operacional de operador dentro do dispositivo, auditoria opcional via `HistoricoStatusPedido.alteradoPorUsuario`. Possíveis evoluções futuras, não implementadas: PIN de operador (UX mais rápida que email/senha para trocar de operador com frequência), auditoria visível no painel Admin (hoje só aparece no detalhe do pedido via `alteradoPorUsuarioNome`, sem tela dedicada), e decidir se vale a pena expor "quantas ações cada operador fez" como relatório.
6. **TASK-110** implementou o modal reutilizável (`components/ui/Modal.tsx`) e migrou Restaurantes/Dispositivos/Categorias/Produtos/Usuários para o padrão lista-primeiro + modal de criação/edição, com login administrativo centralizado. Recomendado como próximo passo: (a) estender `*.test.tsx` do novo padrão modal para as 4 páginas restantes (hoje só `AdminRestaurantesPage` tem teste, o código das demais é análogo); (b) considerar um spec Playwright mockado cobrindo "lista → Novo → modal → salvar → lista atualizada" para pelo menos uma tela Admin, fechando a lacuna "cobertura Playwright de telas Admin" citada acima; (c) avaliar upgrade do focus trap do `Modal` (hoje básico, via `Tab`/`Shift+Tab` manual) se o projeto adotar uma navegação por teclado mais exigente.
7. **TASK-111** fechou uma lacuna de segurança/UX real: `/caixa`/`/cozinha` mostravam a fila operacional inteira só com o dispositivo ativado — o operador (TASK-092) era usado apenas para auditoria opcional, nunca como gate de acesso. Agora as duas telas só buscam/mostram dados depois do login do operador (`CaixaHomePage`/`CozinhaHomePage`, gate client-side) **e** o backend passou a exigir `X-Operador-Token` nas duas listagens (`OperadorContextService.resolverObrigatorio`, novo) — a ocultação no frontend não bastava sozinha. `mvn test` 353/353, `npm test` 53/53, `npm run build`/`npx oxlint` limpos, `npm run e2e` 7/7 (specs de Caixa/Cozinha atualizados para logar operador antes de checar a lista). Não validado nesta task: `npm run e2e:integrado` (specs de Caixa/Cozinha já logavam operador via UI antes desta mudança, então devem continuar passando sem alteração, mas não foi executado — evitar depender de um backend local já em uso para outro fim).
8. **TASK-112** corrigiu um efeito colateral não intencional da TASK-111: sem dispositivo ativado, `/caixa`/`/cozinha` apenas redirecionavam silenciosamente para `/ativar-dispositivo` (sem card, sem botão), e não havia nenhuma forma de trocar de equipamento a partir dessas telas depois de ativadas. Novo `DispositivoAcessoCard` (card centralizado) cobre "sem dispositivo" e "dispositivo de outro tipo" (citando o tipo atual); `OperadorPainel` ganhou a ação "Trocar dispositivo" (com confirmação); novo hook `useDispositivoOperacional` centraliza esse estado (dispositivo virou estado React reativo, não só lido uma vez — corrige telas que ficavam presas em conteúdo antigo após uma sessão de dispositivo invalidada no meio de uma ação) e a nova `tokenStorage.limparSessaoOperacionalCompleta()` (limpa só dispositivo+operador, nunca a sessão administrativa). Investigado e confirmado sem necessidade de mudança: `AtivarDispositivoPage` (já limpava operador residual, já redirecionava pelo tipo retornado) e `TotemHomePage` (já tinha card de erro com botão de ativação). `npm test` 66/66, `npm run build`/`npx oxlint` limpos, `npm run e2e` 10/10 (7 anteriores + 3 novos em `e2e/ativacao-dispositivo.spec.ts`). Backend não alterado, `mvn test` não executado (não necessário). `npm run e2e:integrado` não executado nesta task — backend local em uso para outro fim e sem credencial de `SUPER_ADMIN` conhecida para essa instância; os specs integrados de Caixa/Cozinha continuam com a mesma sequência de login (dispositivo real + operador real via UI antes de checar pedidos), sem dependência de nenhum comportamento alterado nesta task, então a expectativa é que continuem passando sem alteração.
9. **TASK-113** usou a skill `ui-ux-pro-max` para revisar a interface e corrigiu contraste/touch targets/foco/movimento reduzido, sem substituir o Design System: `--color-primary-button` (novo, só fundo de elementos preenchidos) atinge 4.5:1 nos dois temas onde `--color-primary` puro ficava em 4.31/3.99:1 (escurecer `--color-primary` em si pioraria seu uso como texto em ~35 lugares); `--color-text-muted` no tema claro corrigido de 4.12:1 para 5.19:1; `.cart-item-row__remover`/`.cart-summary__aviso` migrados de `--color-text-soft` (2.38:1) para `--color-text-muted`; touch targets de `.theme-toggle`/`.ui-modal__fechar`/`.cart-item-row__qtd-botao` (28×28px→44×44px); `:focus-visible` unificado para `button`/`a`/`input`/`select`/`textarea` (antes só inputs tinham foco customizado); `@media (prefers-reduced-motion: reduce)` global (não existia). Só CSS/tokens — nenhum componente TSX, backend, regra de negócio ou contrato alterado. `npm test` 66/66, `npm run build`/`npx oxlint` limpos, `npm run e2e` 14/14 (10 anteriores + 4 novos em `e2e/acessibilidade.spec.ts`, que confirmam no navegador real — não só no CSS — que os alvos de toque atingem ≥44px e que os elementos continuam funcionais). Erro inline de formulário e contraste de `--color-primary` como texto ficam para task futura (fora do escopo desta). Ver `docs/design-system/cores.md` seção "Contraste (TASK-113)" para os valores medidos.
10. **TASK-114** usou a skill `ui-ux-pro-max` para padronizar botões/ícones/badges/cards, sem substituir o Design System nem criar biblioteca de ícones (zero SVG no projeto, volume não justifica). Achado principal: 5 classes CSS quase-idênticas para "botão fantasma" (2 já fundidas por seletor múltiplo, 3 duplicadas de fato), uma delas (`operador-painel__trocar`) com touch target abaixo de 44px que a TASK-113 não cobriu. `Button.tsx` ganhou `variant` (`primary`/`secondary`/`danger`) + `fullWidth` + `aria-busy`; ~24 usos de `<button>` cru migrados para `<Button variant=...>` nos 5 pares Form/Card administrativos, `PedidoPendenteCard`, `CartSummary` e `OperadorPainel` (corrigindo o touch target de brinde); "Confirmar nova senha" em `UsuarioCard` deixou de parecer um botão secundário quando na verdade é a ação principal do mini-formulário. Badge `.pedido-pendente-card__status` deduplicado contra `.dispositivo-card__status` via CSS (zero mudança de TSX/visual). Ícones: `aria-hidden` nos glifos `×`/`+`/`−`; token `--icon-size-md` unifica o tamanho dos 3 botões só-ícone. Um teste Playwright novo pegou uma regressão real (padding errado causando scroll horizontal em mobile) introduzida durante a própria implementação — corrigida antes de finalizar. `npm test` 74/74 (+8 novos em `Button.test.tsx`), `npm run build`/`npx oxlint` limpos, `npm run e2e` 17/17 (14 anteriores + 3 novos). Bundle CSS **encolheu** (36,55kB→35,17kB), confirmando a deduplicação real. Backend não tocado. `npm run e2e:integrado` não executado (mesma razão das TASK-111/112 — sem impacto esperado, já que nenhum contrato/fluxo mudou). Ver `docs/design-system/componentes.md` para o inventário completo pós-padronização.

11. **TASK-115** fechou a pendência mais antiga do roadmap de UX (adiada nas TASK-113/114): os 5 formulários administrativos (Restaurante/Dispositivo/Categoria/Produto/Usuário) só mostravam uma mensagem de erro global no fim do formulário — sem indicar qual campo, por quê, nem se o erro era de validação local ou vindo da API. `Input.tsx` ganhou `forwardRef` + `error`/`helpText` (`aria-invalid`/`aria-describedby`); novo `FieldError.tsx` (`aria-live="polite"`, não `role="alert"` — reservado à mensagem global). Cada formulário passou a validar **todos** os campos no submit (não mais parar no primeiro `if`), revalidar só o campo já marcado inválido ao corrigir, e mover o foco automaticamente para o primeiro campo inválido (`utils/validacaoFormulario.ts#focarPrimeiroErro`, refs explícitos por formulário — nunca `document.querySelector` global, por segurança com múltiplos modais na página). Achado real: o backend já retornava erro de campo estruturado (`ApiErrorResponse.errors: [{campo, mensagem}]`, `GlobalExceptionHandler`) em toda validação `@Valid` rejeitada, nunca consumido pelo frontend antes — `utils/apiFieldErrors.ts#extrairErrosCampoApi` passou a mapear isso para inline sem adivinhação textual (nomes de campo Java/TypeScript batem 1:1, confirmado lendo os DTOs), preservando a mensagem global (`ErrorMessage`) só para erro de API sem campo reconhecido ou falha de comunicação. Estado por campo: `type CampoX = "a" | "b" | ...` (união literal específica de cada formulário, não uma abstração genérica) + `Partial<Record<CampoX, string>>`, deliberadamente sem biblioteca de formulários (React Hook Form/Formik/Zod/Yup ficaram fora do escopo). Zero mudança de backend/DTO/endpoint/permissão/regra de negócio; upload de imagem do Produto, escopo de restaurante (`restauranteFixo`) e perfis permitidos (`perfisPermitidos`) preservados. `npm test` 104/104 (+30 novos: `FieldError`/`Input`/`RestauranteForm`/`ProdutoForm`/`UsuarioForm`/utils + 4 novos em `AdminRestaurantesPage.test.tsx`), `npm run build`/`npx oxlint` limpos, `npm run e2e` 20/20 (17 anteriores + 3 novos em `e2e/formularios-validacao.spec.ts`, cobrindo Restaurante/Produto/Usuário). `npm run e2e:integrado` não executado — nenhum nome de campo/contrato mudou, só a apresentação do erro; expectativa é que continue passando sem alteração, a confirmar na próxima janela com backend disponível. Ver `docs/design-system/componentes.md` seção "Validação inline nos formulários administrativos" para o padrão completo.
