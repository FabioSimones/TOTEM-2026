# Roadmap Pós-MVP — Totem Fast Food

Criado na TASK-095 (revisão de roadmap e priorização pós-MVP). Este documento não substitui `docs/status-mvp.md` (visão executiva do que já foi feito) nem os checklists detalhados — é a camada seguinte: **o que fazer a partir daqui, em que ordem, e por quê**. Baseado em leitura da documentação existente, do código real (não só do que a documentação descreve) e nos resultados de teste vigentes na data desta revisão (2026-07-13).

## 1. Resumo executivo

O MVP está funcional e bem validado no nível de API: autenticação de usuário e dispositivo com refresh token, fluxo operacional completo (Totem → Pagamento → Caixa → Cozinha → Retirada), painel Admin completo (restaurantes, categorias, produtos, dispositivos, usuários, pedidos, dashboard), escopo por restaurante para `ADMIN_RESTAURANTE`, e uma camada de auditoria por operador humano (Modelo C, TASK-091/092) sobreposta à autenticação por dispositivo. A suíte de testes automatizados é robusta (320/320 no backend) e o CI roda em todo PR/push para `main`.

A maior lacuna não é funcional — é de **evidência visual**: nenhuma tela foi clicada de fato num navegador desde a TASK-086 (Admin) para o fluxo Caixa/Cozinha/operador; toda a validação pós-TASK-086 desse fluxo foi feita via `curl` equivalente, por ausência de ferramenta de automação de navegador neste ambiente (bloqueio reconfirmado na TASK-094.1, não é bug). Isso não bloqueia uma demo interna, mas é um risco real antes de um deploy de produção ou de uma demonstração para stakeholders que vão realmente tocar na tela.

Além disso, há um conjunto de decisões de MVP conscientemente simplificadas (JWT secret com fallback de desenvolvimento, CORS com origens fixas em `localhost`, seed de `SUPER_ADMIN` com senha fixa documentada publicamente, rate limit em memória, uploads em disco local, `FakePaymentProvider`, sem estorno) que são **aceitáveis para um MVP de demonstração, mas inaceitáveis sem tratamento antes de um ambiente de produção real com dinheiro de verdade**.

## 2. Estado atual do MVP

**Funcional (ponta a ponta, todos os módulos)**:
- Autenticação de usuário (login/refresh/logout/rate limit) e de dispositivo (ativação/refresh/revogação/regeneração de código).
- Fluxo operacional Totem → Caixa → Cozinha → Retirada, com Pix/cartão simulados (aprovação automática) e dinheiro (confirmação manual no Caixa).
- Cancelamento de pedido (até `ENVIADO_PARA_COZINHA`) e expiração automática de pedidos não pagos.
- Admin completo: restaurantes, categorias, produtos (com upload de imagem), dispositivos (com status operacional derivado), usuários, pedidos (paginado, só leitura), dashboard (contadores).
- Escopo por restaurante para `ADMIN_RESTAURANTE` em todos os módulos administrativos.
- Operador humano (Modelo C): login operacional dentro do dispositivo, header opcional `X-Operador-Token`, auditoria no histórico do pedido (`alteradoPorUsuario`/`alteradoPorDispositivo`).

**Testado automaticamente**: 320/320 testes backend (`mvn test`, H2 em memória) + 5/5 testes contra PostgreSQL real via Testcontainers (`mvn verify -Ppostgres-it`, cobrindo especificamente fuso horário e expiração de pedidos, os dois pontos onde bugs reais já apareceram). CI (`ci.yml`) roda os dois perfis de backend + build/lint do frontend em todo PR/push para `main`. **Frontend não tem nenhum teste automatizado** — só `tsc`/`oxlint`.

**Validado por API/curl** (equivalente funcional ao clique real, reproduzindo headers/sequência exatos do frontend): toda a matriz de autenticação e autorização do operador (perfis compatíveis/incompatíveis, escopo de restaurante, token inválido/incompatível/expirado, troca de operador, limpeza seletiva vs. completa de sessão), refresh de dispositivo, regeneração de código de ativação, CORS (preflight simulado via `curl`).

**Validado visualmente (clique real em navegador)**: só o painel Admin (login SUPER_ADMIN/ADMIN_RESTAURANTE, Home, Dashboard, Pedidos, Dispositivos, Produtos, Categorias, Restaurantes, Usuários, refresh automático de sessão) — TASK-086. **Nunca clicado de verdade**: Totem, Caixa, Cozinha, ativação de dispositivo, e todo o fluxo de operador (TASK-092/093/094/094.1) — 4 tentativas registradas, todas bloqueadas pela mesma ausência de ferramenta de automação de navegador neste ambiente.

**Fluxos principais prontos para uso real** (assumindo pagamento real for tratado à parte): Totem→Caixa→Cozinha→Retirada, gestão de cardápio, gestão de dispositivos, gestão de usuários/operadores, consulta administrativa de pedidos.

## 3. Pendências conhecidas

Confirmadas por leitura de código nesta revisão (não apenas repetindo o que a documentação já dizia):

| Item | Estado real confirmado | Fonte |
|---|---|---|
| Homologação visual real (Caixa/Cozinha/operador) | Pendente — sem ferramenta de navegador neste ambiente, 4 tentativas bloqueadas (TASK-089/092-094/094.1) | `docs/checklists/fluxo-operacional-mvp.md`, `docs/testes-backend-mvp.md` |
| PIN de operador | Não implementado — decisão consciente da TASK-092 (email/senha, token sem refresh) | `docs/checklists/fluxo-operacional-mvp.md` §10 |
| Dashboard/auditoria por operador | Não existe — auditoria só aparece no detalhe do pedido (`alteradoPorUsuarioNome`), sem agregação/relatório por operador | `DashboardAdminService.java`, `AdminDashboardPage.tsx` (nenhuma referência a operador) |
| Testes frontend (Vitest) | Inexistente — `frontend/package.json` não tem Vitest/Jest/Testing Library | `frontend/package.json` |
| Testes E2E (Playwright/Cypress) | Inexistente — nenhuma dependência, nenhuma ferramenta de automação de navegador disponível neste ambiente de desenvolvimento | verificado nesta task |
| WebSocket/SSE | Inexistente — Totem usa polling leve de 15s; Caixa/Cozinha dependem de atualização manual | `docs/11-fluxos.md` |
| CORS externalizado por ambiente | **Resolvido na TASK-098** — origens vêm de `CORS_ALLOWED_ORIGINS`; `CorsOriginsValidator` falha o startup se ausente, com `*`, ou sem protocolo explícito | `SecurityConfig.java`, `config/CorsOriginsValidator.java` |
| JWT secret sem fallback frágil em produção | **Resolvido na TASK-097** — fallback removido de `application.yml`; `JwtSecretValidator` falha o startup se ausente, curto (<32) ou igual ao valor antigo | `application.yml`, `security/JwtSecretValidator.java` |
| Rate limit em memória | Confirmado — `LoginAttemptService`, contadores zeram a cada reinício, não substitui WAF/rate limit de borda | `docs/08-endpoints.md` |
| Uploads em disco local | Confirmado — `app.uploads.dir`, sem storage externo (S3/Cloudinary) | `application.yml:75-78` |
| Contratos `LocalDateTime` sem offset | Confirmado — mitigado no frontend (`utils/dateTime.ts` assume UTC), backend continua sem `Instant`/`OffsetDateTime` | `docs/09-contratos-api.md` |
| Dashboard "hoje" em UTC vs. `America/Sao_Paulo` | Confirmado, decisão deliberada mantida desde a TASK-074 | `docs/status-mvp.md` |
| Fake payment provider | Confirmado — sem Pix/TEF/SmartPOS real | `docs/09-contratos-api.md` |
| Estorno/cancelamento de pedido pago | Confirmado — cancelamento de pedido `PAGO` não estorna o pagamento (`Pagamento` permanece `AUTORIZADO`), decisão documentada | `CaixaPedidoService.java:132` |
| Seed `SUPER_ADMIN` com senha fixa | **Resolvido na TASK-096** — `V7` desativa a conta com a senha fixa (`Admin@2026!`) nunca trocada; `SuperAdminBootstrapRunner` passa a ser o único caminho para criar o primeiro `SUPER_ADMIN`, sem valor padrão de senha em nenhum lugar | `db/migration/V7__desativar_seed_super_admin_conhecido.sql`, `bootstrap/SuperAdminBootstrapRunner.java` |
| Entidade Rede/Grupo de restaurantes | Não implementada — modelo atual é restaurante único, sem hierarquia | não encontrada no schema |
| Multi-tenant por rede | Não implementado — escopo atual é só por `restauranteId` | idem |
| Observabilidade/logs | **Resolvido na TASK-099** — `spring-boot-starter-actuator` com `/actuator/health`/`/actuator/info` públicos (demais endpoints não expostos); logs INFO/WARN nos fluxos sensíveis (login, ativação/refresh de dispositivo, ações de Caixa/Cozinha, bootstrap de SUPER_ADMIN), sem token/senha/payload sensível. Ainda sem log estruturado (JSON)/correlação/Prometheus/tracing — fora do escopo da TASK-099 | `backend/pom.xml`, `application.yml`, `SecurityConfig.java`, `docs/04-seguranca.md` |
| Backup/restore | Não documentado nem automatizado | não encontrado |
| Deploy | Não há Dockerfile/docker-compose/manifesto de deploy no repositório | verificado nesta task |
| CI/CD | CI existe (`ci.yml`, 3 jobs); **CD não existe** (nenhum deploy automatizado) | `.github/workflows/ci.yml` |
| Branch protection | Não configurada — já registrada como pendência desde a TASK-084 | `docs/status-mvp.md` §"Próximas tasks recomendadas" item 2 |
| Testcontainers ampliado | Cobre só fuso horário e expiração; fluxo operacional completo e demais módulos administrativos continuam só H2 + validação manual contra Postgres | `docs/status-mvp.md` |
| Documentação de operação (runbook) | Não existe — a documentação é forte em arquitetura/contrato/decisão, mas não há um "o que fazer se X quebrar em produção" | verificado nesta task |

## 4. Priorização P0/P1/P2/P3

### P0 — impede operação real / risco grave de segurança / perda de dados
- ~~**Seed `SUPER_ADMIN` com senha fixa documentada publicamente**~~ **resolvido na TASK-096** — `V7` desativa a conta com a senha nunca trocada; criação do primeiro `SUPER_ADMIN` passou a ser via `SuperAdminBootstrapRunner`, sem senha padrão em código.
- ~~**JWT secret com fallback silencioso para valor de desenvolvimento**~~ **resolvido na TASK-097** — sem `JWT_SECRET` (ou com um valor curto/o antigo conhecido), a aplicação agora falha no startup em vez de assinar tokens com uma chave comprometida por definição.
- **CORS com origens fixas em `localhost`** — não é só uma melhoria: em produção, o frontend real (domínio diferente de `localhost:5173`) será bloqueado pelo próprio CORS que hoje "funciona" só porque ambiente de dev e produção nunca foram diferenciados. Sem isso, o sistema **não funciona** fora do laptop de desenvolvimento.

### P1 — importante para operação/segurança, mas com workaround; melhora confiabilidade
- Homologação visual real do fluxo Caixa/Cozinha/operador (workaround atual: validação funcional via `curl`, já ampla, mas sem confirmação de UX real).
- Rate limit em memória (workaround: aceitável para instância única; quebra com múltiplas instâncias/reinícios frequentes).
- ~~Observabilidade/logs mínima~~ **resolvido na TASK-099** — Actuator health/info + logs operacionais nos fluxos sensíveis.
- Testcontainers ampliado para o fluxo operacional completo (workaround: H2 já cobre a lógica, Postgres real só foi testado nos dois pontos historicamente problemáticos).
- Branch protection no GitHub (workaround: disciplina manual da equipe).
- Uploads em disco local (workaround: aceitável para volume baixo/single-instance; quebra se houver múltiplas instâncias sem storage compartilhado).

### P2 — melhoria de produto/UX, dívida técnica relevante, prepara escala
- Testes automatizados de frontend (Vitest).
- Testes E2E (Playwright/Cypress) — depende de decidir/instalar ferramenta, hoje ausente do ambiente de desenvolvimento também.
- Dashboard/auditoria por operador (relatório "quantas ações cada operador fez").
- WebSocket/SSE para Caixa/Cozinha (troca polling por atualização em tempo real).
- Contratos `LocalDateTime` sem offset (migração para `Instant`/`OffsetDateTime`).
- Documentação de operação (runbook).
- Entidade Rede/Grupo de restaurantes e multi-tenant por rede (só relevante se houver plano real de vender para múltiplas redes).

### P3 — nice-to-have, polimento, otimização futura
- PIN de operador (troca de operador mais rápida que email/senha).
- Dashboard "hoje" em `America/Sao_Paulo` em vez de UTC.
- Estorno automático no cancelamento de pedido pago (hoje é decisão de produto deliberada, não bug).
- Fake payment provider → gateway real (Pix/TEF/SmartPOS) — grande o suficiente para ser uma iniciativa própria, não uma "melhoria".
- Backup/restore automatizado, deploy/CD.

## 5. Roadmap recomendado

A sequência lógica é: **fechar os 3 riscos P0 (que são configuração/seed, não feature) → decidir e instrumentar observabilidade básica → só então investir em automação de teste (Vitest/E2E) → só então evoluir produto (WebSocket, dashboard de operador, PIN) → deixar Rede/multi-tenant e integrações de pagamento real para quando houver demanda de negócio concreta**, porque essas duas últimas mudam modelo de dados e regra financeira e não devem ser antecipadas sem necessidade.

## 6. Próximas 10 tasks sugeridas

| # | Título | Categoria | Prioridade | Objetivo | Arquivos prováveis | Validações esperadas | Por que nessa ordem |
|---|---|---|---|---|---|---|---|
| TASK-096 | ~~Seed de `SUPER_ADMIN` seguro~~ **concluída** | Segurança | P0 | Eliminar a senha fixa documentada publicamente — feito via `V7` (desativação condicional) + `SuperAdminBootstrapRunner` (criação controlada por variável de ambiente, sem default de senha) | `V7__desativar_seed_super_admin_conhecido.sql`, `bootstrap/SuperAdminBootstrapRunner.java`, `UsuarioRepository.java`, `application.yml` | `mvn test` → 324/324 BUILD SUCCESS (320 + 4 novos) | Era o risco mais barato de mitigar e o mais grave — feito antes de qualquer outra coisa tocar em auth |
| TASK-097 | ~~Falha rápida se `JWT_SECRET` não for setado~~ **concluída** | Segurança | P0 | Impedir que a aplicação suba com a chave de desenvolvimento antiga — feito via remoção do fallback + `JwtSecretValidator` (sem profile novo: `JWT_SECRET` passou a ser obrigatório inclusive em dev, para não reabrir o mesmo risco por outro caminho) | `application.yml`, `security/JwtSecretValidator.java`, `security/JwtService.java` | `mvn test` → 330/330 BUILD SUCCESS (324 + 6 novos) | Mesma classe de risco da TASK-096 (segredo público comprometendo produção), feita logo em seguida por ser mudança pequena e isolada em security config |
| TASK-098 | ~~Externalizar origens de CORS por variável de ambiente~~ **concluída** | Segurança/Deploy | P0 | `allowedOrigins` deixou de ser hardcoded — feito via `CORS_ALLOWED_ORIGINS` (sem fallback) + `CorsOriginsValidator` (rejeita `*`/ausência/origem sem protocolo) | `SecurityConfig.java`, `config/CorsOriginsValidator.java`, `application.yml` | `mvn test` → 341/341 BUILD SUCCESS (330 + 11 novos, incluindo o primeiro teste de integração de CORS do projeto) | Fechou o último P0 — sem isso o sistema não rodava fora de `localhost` de jeito nenhum |
| TASK-099 | ~~Observabilidade mínima (Actuator + logs operacionais)~~ **concluída** | Infra | P1 | Adicionado `spring-boot-starter-actuator` (`/actuator/health`, `/actuator/info` públicos, demais endpoints não expostos) + logs INFO/WARN nos fluxos sensíveis restantes sem log (`CozinhaPedidoService`, bootstrap de SUPER_ADMIN desabilitado) | `pom.xml`, `application.yml`, `SecurityConfig.java`, `CozinhaPedidoService.java`, `SuperAdminBootstrapRunner.java` | `mvn test` → 347/347 BUILD SUCCESS (341 + 6 novos) | Depois dos P0 de segurança, era o que mais reduzia "voar às cegas" antes de qualquer deploy real |
| TASK-100 | ~~Branch protection + badge de CI~~ **documentação concluída** | DevOps | P1 | Exigir os 3 jobs de `ci.yml` como check obrigatório antes de merge em `main`; badge no `README.md` — feito: badge em `README.md`, checklist completo em `docs/ci-branch-protection.md`. **Ação manual pendente**: aplicar a regra em Settings → Branches no GitHub (exige acesso administrativo, fora do escopo desta task) | `README.md`, `docs/ci-branch-protection.md`, `docs/status-mvp.md` | Nenhuma (documentação/config de repositório) | Baixo esforço, alto valor de proteção — já estava pendente desde a TASK-084 |
| TASK-101 | ~~Configurar Vitest + Testing Library no frontend~~ **concluída** | Testes | P2 | Primeira suíte de teste automatizado de frontend — feito: 37 testes cobrindo `tokenStorage.ts`, `dateTime.ts`, `adminScope.ts`, `OperadorPainel.tsx` e a renovação automática de sessão em `api.ts` | `frontend/package.json`, `frontend/vite.config.ts` (bloco `test`, sem arquivo `vitest.config.ts` separado), `frontend/src/test/setup.ts` (novo), `frontend/src/**/*.test.ts(x)` (novos), `.github/workflows/ci.yml` (`npm test` no job frontend) | `npm test` → 37/37; `npm run build`; `npm run lint` | Precedia qualquer refatoração de frontend — `api.ts`/`tokenStorage.ts` (código sensível de sessão) agora têm rede de segurança |
| TASK-102 | ~~Homologação visual real (Playwright headless) — Caixa/Cozinha/operador~~ **concluída** | Testes/Validação | P1 | Fechar a pendência da TASK-094.1 com clique real automatizado, API mockada (`page.route`) — feito: 7 testes cobrindo login Admin, `OperadorPainel` (identificar/trocar) e carregamento visual de Totem/Caixa/Cozinha; achou e corrigiu um bug real de CSS (`.cart-summary` com largura errada em desktop) | `frontend/package.json`, `frontend/playwright.config.ts` (novo), `frontend/e2e/*.spec.ts` (novos), `frontend/e2e/helpers/*.ts` (novos), `frontend/src/styles/global.css` (bugfix mínimo), `frontend/vite.config.ts` (`test.include` para não colidir com Vitest), `.gitignore` | `npm run e2e` → 7/7; `npm test` → 37/37; `npm run build`; `npm run lint` | Só fazia sentido depois que Vitest já estabeleceu a convenção de testes frontend no projeto; usa o mesmo Node já existente. **CI**: Playwright não entrou no `ci.yml` nesta task (decisão deliberada) |
| TASK-103 | ~~Playwright no CI~~ **concluída** | DevOps/Testes | P1 | Fechar a pendência deixada pela TASK-102: job próprio `frontend-e2e` (`Frontend E2E (Playwright)`) em `ci.yml`, com `needs: frontend`, instalando só Chromium e publicando `playwright-report`/`test-results` como artifact em falha | `.github/workflows/ci.yml`, `frontend/README.md`, `docs/status-mvp.md`, `docs/roadmap-pos-mvp.md`, `docs/testes-backend-mvp.md`, `docs/ci-branch-protection.md` | `npm run e2e` → 7/7 (local, mesmo comando do CI); `npm test`/`npm run build`/`npm run lint` sem regressão | Baixo esforço depois que a suíte mockada da TASK-102 já estava estável localmente; `needs: frontend` evita gastar runner instalando browser se o básico do frontend já estiver quebrado |
| TASK-103.1 | ~~Validar primeiro run real do Playwright no GitHub Actions~~ **concluída — tudo verde** | DevOps/Testes | P1 | Confirmar que o job `frontend-e2e` funciona no runner real (`ubuntu-latest`), não só localmente — feito: run `29361958515` (commit `0f98475`), os 4 jobs `success`, 0 artifacts gerados (esperado sem falha) | Nenhum (só documentação: `docs/status-mvp.md`, `docs/roadmap-pos-mvp.md`) | Consulta à API do GitHub Actions (`gh` indisponível neste ambiente, usado `curl`/API REST) | Sem isso, `Frontend E2E (Playwright)` era uma suposição de que funcionaria no CI, nunca observada de fato — diferença de SO (Linux vs Windows local) já causou um bug real antes (TASK-102/TASK-099.1) |
| TASK-104 | ~~E2E integrado com backend real (Totem, Fluxo A)~~ **concluída** | Testes/Validação | P1 | Primeira suíte E2E sem mocks — feito: `frontend/e2e-integrado/totem-pedido-real.spec.ts` (login SUPER_ADMIN real, cria restaurante/categoria/produto/dispositivo via API real, ativa dispositivo, abre `/totem`, cardápio real, cria pedido real, paga com Pix, confirma `AUTORIZADO`/`PAGO`); validado de ponta a ponta contra PostgreSQL+backend reais locais | `frontend/package.json`, `frontend/playwright.integrado.config.ts` (novo), `frontend/e2e-integrado/*` (novo), `.gitignore` | `npm run e2e:integrado` → 1/1 (backend real); `npm run e2e` → 7/7 (mockada, sem regressão); `npm test`/`npm run build`/`npm run lint` sem regressão | Só o Totem (Fluxo A) — Caixa/Cozinha/operador com backend real cobertos na TASK-105 (imediatamente em seguida); não entrou no CI (decisão desta task) |
| TASK-105 | ~~E2E integrado com backend real (Caixa/Cozinha/operador)~~ **concluída** | Testes/Validação | P1 | Fecha a lacuna deixada pela TASK-104 — feito: `frontend/e2e-integrado/caixa-cozinha-operador-real.spec.ts` (dois `BrowserContext` simulando dois terminais, operadores reais identificados via UI, pedido `PAGO`→`RETIRADO` por clique real, histórico de auditoria `alteradoPorUsuarioNome`/`alteradoPorDispositivoNome` validado via API); pedido preparado via API do Totem, não pela UI (já coberta na TASK-104) | `frontend/e2e-integrado/helpers/backendApi.ts` (funções aditivas), `frontend/e2e-integrado/caixa-cozinha-operador-real.spec.ts` (novo) | `npm run e2e:integrado` → 2/2 (backend real); `npm run e2e` → 7/7 (mockada); `npm test`/`npm run build`/`npm run lint` sem regressão | Reaproveitou os helpers e a config da TASK-104 — baixo esforço incremental; fecha a mesma pendência histórica de "clique real" (TASK-094.1) para os terminais que faltavam |
| TASK-106 | ~~Automatizar E2E integrado no CI~~ **concluída** | DevOps/Testes | P1 | Job `frontend-e2e-integrado` no `ci.yml` — feito: PostgreSQL via `services:`, backend real via `mvn spring-boot:run` em background, espera por `/actuator/health`, `npm run e2e:integrado`, artifact em falha (`playwright-report-integrado` + log do backend) | `.github/workflows/ci.yml`, `frontend/README.md`, `docs/ci-branch-protection.md`, `docs/status-mvp.md`, `docs/roadmap-pos-mvp.md`, `docs/testes-backend-mvp.md`, `docs/checklists/fluxo-operacional-mvp.md` | Validado localmente reproduzindo o mesmo padrão do job (Postgres+backend via `nohup`/`disown`) → `npm run e2e:integrado` 2/2; `npm test`/`npm run build`/`npm run lint`/`npm run e2e` sem regressão | `needs: [frontend, frontend-e2e, backend-postgres-it]` — gate atrás dos jobs baratos, já que é o mais caro do workflow; não virou check obrigatório (precisa de runs reais primeiro, como o `Frontend E2E (Playwright)` na TASK-103.1) |
| TASK-109 | Runbook de operação (documentação) | Documentação | P2 | Documento único: como reiniciar, como rotacionar `JWT_SECRET`, como restaurar backup, o que fazer se o rate limit travar um admin legítimo | `docs/15-runbook-operacao.md` (novo) | Nenhuma (documental) | Depende das decisões de observabilidade (TASK-099) e segredo (TASK-096/097) já estarem tomadas, para não documentar algo que vai mudar |
| TASK-107 | Dashboard/relatório de ações por operador | Produto | P2 | Nova consulta administrativa: quantas ações cada operador executou, por período/restaurante | `DashboardAdminService.java` ou novo `OperadorRelatorioService`, `AdminDashboardPage.tsx` ou nova tela | `mvn test` novo, `npm run build` | Só depois da base de testes (Vitest) existir, para a primeira feature nova de produto pós-MVP já nascer testada |
| TASK-108 | WebSocket/SSE para Caixa/Cozinha (substituindo polling) | Produto/Arquitetura | P2 | Atualização em tempo real das filas de Caixa/Cozinha, mantendo REST como fallback | `backend`: novo módulo de mensageria; `frontend`: `CaixaHomePage.tsx`/`CozinhaHomePage.tsx` | `mvn test`, `npm run build`, teste manual de latência | Mudança de arquitetura relevante — deve vir só depois que a base de testes (Vitest + E2E) já existir para não regressar o fluxo operacional sem rede de segurança |
| TASK-110 | ~~Refatoração visual do login e cadastros administrativos~~ **concluída** | Produto/UX | P2 | Login administrativo centralizado (`AppLayout.centralizado`); novo `components/ui/Modal.tsx` reutilizável (acessível, Escape, foco, bloqueio de scroll); Restaurantes/Dispositivos/Categorias/Produtos/Usuários migrados de "formulário sempre visível" para "lista + botão Novo... + modal", reaproveitando os `*Form.tsx` existentes sem mudar regras/contrato | `frontend/src/components/ui/Modal.tsx` (novo) e `Modal.test.tsx` (novo), `frontend/src/components/layout/AppLayout.tsx`, `frontend/src/pages/admin/Admin{Login,Restaurantes,Dispositivos,Categorias,Produtos,Usuarios}Page.tsx`, `frontend/src/components/admin/**/*Form.tsx` (remoção do `<h2>` de título interno, botão Cancelar sempre visível), `frontend/src/styles/global.css`, `AdminRestaurantesPage.test.tsx` (novo) | `npm test` → 47/47; `npm run build`; `npx oxlint` limpo; `npm run e2e` → 7/7 (sem regressão); captura de tela manual (login e modal, desktop/mobile) | UX pura, sem tocar backend/contrato/permissões — podia ser feita a qualquer momento; entrou aqui por ser a pendência de experiência mais visível do painel Admin no MVP |
| TASK-111 | ~~Bloquear Caixa e Cozinha até login do operador~~ **concluída** | Segurança/UX | P1 | Fechou uma lacuna real: dispositivo CAIXA/COZINHA ativado, sozinho, já mostrava a fila operacional inteira — operador (TASK-092) era só auditoria opcional. Frontend: `CaixaHomePage`/`CozinhaHomePage` só buscam dados com operador identificado, mostrando só o card de login centralizado antes disso (reaproveita `AppLayout.centralizado` da TASK-110). Backend: `OperadorContextService.resolverObrigatorio` (novo) passou a ser exigido nas duas listagens (`GET /api/caixa/pedidos/pendentes`, `GET /api/cozinha/pedidos`) — decisão explícita de não confiar só na ocultação do frontend; as 4 ações de escrita continuam com operador opcional, sem mudança | `backend`: `OperadorContextService.java`, `CaixaPedidoController.java`, `CozinhaPedidoController.java`, `OperadorLoginIntegrationTest.java`, `FluxoOperacionalMvpIntegrationTest.java`; `frontend`: `OperadorPainel.tsx`, `CaixaHomePage.tsx`, `CozinhaHomePage.tsx` (+ `.test.tsx` novos para as duas), `global.css`, `e2e/fluxo-operacional-mock.spec.ts` | `mvn test` → 353/353; `npm test` → 53/53; `npm run build`; `npx oxlint` limpo; `npm run e2e` → 7/7 | Risco de segurança real, não hipotético — qualquer um com acesso físico a um terminal CAIXA/COZINHA ativado via dispositivo veria nome de cliente/pedido antes de qualquer humano se identificar |
| TASK-112 | ~~Recuperar ativação de dispositivo nas telas operacionais~~ **concluída** | UX/Produto | P1 | Corrigiu efeito colateral não intencional da TASK-111: sem dispositivo, `/caixa`/`/cozinha` só redirecionavam silenciosamente para `/ativar-dispositivo` (sem botão visível), e não havia forma de trocar de equipamento pela tela. Novo `DispositivoAcessoCard` (card centralizado "Ativar este dispositivo"/"Trocar dispositivo", citando o tipo atual quando incompatível); `OperadorPainel` ganhou ação "Trocar dispositivo" (com confirmação); novo hook `useDispositivoOperacional` (dispositivo virou estado React reativo) e `tokenStorage.limparSessaoOperacionalCompleta()` (limpa só dispositivo+operador, nunca sessão administrativa) | `frontend/src/hooks/useDispositivoOperacional.ts` (novo), `frontend/src/components/dispositivo/DispositivoAcessoCard.tsx` (novo), `frontend/src/components/operador/OperadorPainel.tsx`, `frontend/src/pages/caixa/CaixaHomePage.tsx`, `frontend/src/pages/cozinha/CozinhaHomePage.tsx` (+ `.test.tsx` de ambos ampliados), `frontend/src/services/tokenStorage.ts` (+ `.test.ts`), `frontend/src/utils/tipoDispositivo.ts` (novo), `frontend/src/styles/global.css`, `frontend/e2e/ativacao-dispositivo.spec.ts` (novo) | `npm test` → 66/66; `npm run build`; `npx oxlint` limpo; `npm run e2e` → 10/10; `mvn test` não necessário (backend não alterado); `npm run e2e:integrado` não executado (backend local em uso para outro fim, sem credencial de SUPER_ADMIN conhecida) | Backend não alterado — `AtivarDispositivoPage`/`TotemHomePage` já satisfaziam os requisitos investigados (limpeza de operador residual, redirect por tipo, card de erro com botão); risco de regressão baixo por reaproveitar o padrão visual centralizado das TASK-110/111 |
| TASK-113 | ~~Acessibilidade visual e touch targets~~ **concluída** | UX/Acessibilidade | P1 | Usou a skill `ui-ux-pro-max` para revisar a interface. Corrigiu contraste insuficiente (`--color-primary` vs `--color-primary-contrast` nos botões, `--color-text-muted` no tema claro, `--color-text-soft` usado em texto real do carrinho), touch targets abaixo de 44px (`.theme-toggle`, `.ui-modal__fechar`, `.cart-item-row__qtd-botao`), foco inconsistente (só inputs tinham `:focus` customizado) e ausência de `prefers-reduced-motion`. Decisão-chave: criar `--color-primary-button` em vez de escurecer `--color-primary` — escurecer o token existente resolveria o botão mas pioraria seu uso como texto em ~35 lugares (medido, não seria "não prejudicar tema dark/light") | `frontend/src/styles/themes.css` (`--color-primary-button` novo, `--color-text-muted` light ajustado), `frontend/src/styles/global.css` (5 pontos de fundo preenchido repontados, 2 textos migrados de `--color-text-soft`, touch targets, `:focus-visible` unificado — remove 3 regras `:focus` duplicadas —, bloco `prefers-reduced-motion`), `frontend/e2e/acessibilidade.spec.ts` (novo), `docs/design-system/cores.md` (seção "Contraste" + "Foco visível, toque e movimento") | `npm test` → 66/66 (sem regressão, nenhum TSX alterado); `npm run build`; `npx oxlint` limpo; `npm run e2e` → 14/14 (10 anteriores + 4 novos, que medem `boundingBox()` ≥44px no navegador real); contraste medido antes/depois via fórmula WCAG (ver `docs/design-system/cores.md`) | Zero mudança de componente/lógica — só tokens CSS; erro inline de formulário e contraste de `--color-primary` como texto (não como fundo) ficam para task futura, fora do escopo pedido |
| TASK-114 | ~~Padronização visual de botões, ícones e componentes~~ **concluída** | UX/Consistência | P2 | Usou a skill `ui-ux-pro-max`. Auditoria encontrou 5 classes CSS quase-idênticas para "botão fantasma" (2 já fundidas por seletor múltiplo) e 1 badge duplicada; `operador-painel__trocar` tinha bug real de touch target (~28-32px) que a TASK-113 não cobriu. `Button.tsx` ganhou `variant`(`primary`/`secondary`/`danger`)/`fullWidth`/`aria-busy`, consolidando a CSS duplicada numa base única; ~24 usos de `<button>` cru migrados para `<Button>`; badge `.pedido-pendente-card__status` deduplicado contra `.dispositivo-card__status`; ícones ganharam `aria-hidden` consistente e token `--icon-size-md` único. Zero SVG no projeto — biblioteca de ícones avaliada e descartada (sem volume que justifique). Cards e grupos de ações já estavam consolidados (`.pedido-pendente-card`, `.dispositivo-form__acoes`) — só documentados, não renomeados | `frontend/src/components/ui/Button.tsx` (+ `.test.tsx` novo), `frontend/src/styles/tokens.css` (`--icon-size-*`), `frontend/src/styles/global.css` (variantes de botão, badge deduplicado, ícones), `frontend/src/components/ui/Modal.tsx`, `frontend/src/components/totem/CartItemRow.tsx`/`CartSummary.tsx`, `frontend/src/components/caixa/PedidoPendenteCard.tsx`, `frontend/src/components/operador/OperadorPainel.tsx`, 5 pares `*Form.tsx`/`*Card.tsx` administrativos, `frontend/e2e/acessibilidade.spec.ts` (ampliado), `docs/design-system/componentes.md` | `npm test` → 74/74 (+8 novos); `npm run build` (bundle CSS encolheu 36,55kB→35,17kB, confirmando dedup real); `npx oxlint` limpo; `npm run e2e` → 17/17 (14 anteriores + 3 novos) | Um teste Playwright novo pegou uma regressão real (padding `--spacing-lg` em vez de `--spacing-md` na variante fantasma causando scroll horizontal em mobile) introduzida durante a própria implementação desta task — corrigida antes de finalizar, exatamente o tipo de bug que só aparece testando responsividade de verdade |
| TASK-115 | ~~Validação inline nos formulários administrativos~~ **concluída** | UX/Acessibilidade | P2 | Pendência mais antiga do roadmap de UX (adiada nas TASK-113/114): os 5 formulários administrativos (Restaurante/Dispositivo/Categoria/Produto/Usuário) só tinham uma mensagem global no fim do formulário — sem indicar qual campo, por quê, ou se o erro era local ou da API. `Input.tsx` ganhou `forwardRef`+`error`/`helpText` (`aria-invalid`/`aria-describedby`); novo `FieldError.tsx` (`aria-live="polite"`, não `role="alert"`); cada formulário passou a validar **todos** os campos no submit (`Partial<Record<CampoX, string>>` tipado por união literal, não uma abstração genérica), revalidar só o campo já inválido ao digitar, e focar automaticamente o primeiro campo inválido (`utils/validacaoFormulario.ts#focarPrimeiroErro`, refs explícitos, nunca `querySelector` global — seguro com múltiplos modais). Descoberta-chave: o backend já retornava `ApiErrorResponse.errors: [{campo, mensagem}]` em toda validação `@Valid` rejeitada, nunca consumido pelo frontend — `utils/apiFieldErrors.ts#extrairErrosCampoApi` mapeia isso para erro inline sem adivinhação textual (nomes de campo Java/TS batem 1:1, confirmado lendo os DTOs), mantendo `ErrorMessage`/`role="alert"` só para erro de API sem campo reconhecido ou falha de comunicação. Zero mudança de backend/contrato/regra de negócio; upload de imagem do Produto e escopo de restaurante/perfil preservados | `frontend/src/components/ui/{FieldError.tsx,Input.tsx}` (+ `.test.tsx` novos), `frontend/src/utils/{validacaoFormulario.ts,apiFieldErrors.ts}` (novos, + `.test.ts`), `frontend/src/styles/global.css` (`.ui-field--invalid`/`.ui-field__error`/`.ui-field__help`), `frontend/src/components/admin/**/*Form.tsx` (5 formulários reescritos) + `UsuarioCard.tsx` (mini-formulário de troca de senha), `frontend/src/pages/admin/Admin{Restaurantes,Dispositivos,Categorias,Produtos,Usuarios}Page.tsx` (`errosCampoApi` por página), `frontend/src/test/setup.ts` (polyfill `matchMedia`/`scrollIntoView` para jsdom), `frontend/e2e/formularios-validacao.spec.ts` (novo), `frontend/e2e/helpers/storage.ts` (`seedAdminSession`, novo), `docs/design-system/componentes.md` | `npm test` → 104/104 (+30 novos); `npm run build`; `npx oxlint` limpo (1 aviso pré-existente não relacionado); `npm run e2e` → 20/20 (17 anteriores + 3 novos) | `npm run e2e:integrado` não executado — nenhum contrato/nome de campo mudou (só apresentação do erro), então a expectativa é que continue passando sem alteração; recomenda-se confirmar na próxima janela com backend disponível |
| TASK-116 | ~~Validação e homologação da autenticação centralizada~~ **concluída** | Testes/Segurança | P1 | A centralização de login (`AuthProvider`/`ProtectedRoute`/`RoleGuard`/`resolveHomeRoute`, storage separado USER/DEVICE/OPERADOR) tinha sido implementada sem nenhum build/teste/lint/E2E executado — esta task rodou tudo de verdade e corrigiu a única regressão real encontrada (`e2e/acessibilidade.spec.ts` corria contra o redirect client-side de `/admin/login`, corrigido aguardando o formulário montar antes do `Tab`). Também corrigiu um achado de segurança real do `JwtAuthenticationFilter` (token `OPERADOR` não podia mais autenticar como `USER` via `Authorization: Bearer`) | `backend/.../security/JwtAuthenticationFilter.java`, `backend/.../security/SecurityHttpStatusTest.java`, `frontend/src/auth/*.test.tsx`, `frontend/src/pages/auth/LoginPage.test.tsx`, `frontend/src/pages/totem/TotemHomePage.test.tsx`, `frontend/e2e/login-centralizado.spec.ts` (novo), `frontend/e2e/acessibilidade.spec.ts` (fix) | `mvn test` → 354/354; `npm test` → 142/142; `npm run build`; `npx oxlint` limpo; `npm run e2e` → 30/30 | `npm run e2e:integrado` não executado — sem credenciais `E2E_ADMIN_EMAIL`/`PASSWORD` confirmadas para o backend em execução |
| TASK-117 | ~~Redesign do login e entrada padrão pela ativação de dispositivo~~ **concluída** | UX/Produto | P2 | Usou a skill `ui-ux-pro-max`. `/` passou a redirecionar para `/ativar-dispositivo` (a `HomePage` estática, órfã desde a TASK-115, foi removida); `/login` ganhou um layout dividido (`AuthSplitLayout`/`LoginBrandPanel`/`FoodIcons`, novos em `components/auth/`) com marca "TotemFood", os três primeiros SVGs do projeto (hambúrguer/batata/bebida, decorativos, animação sutil só com `transform`/`opacity`, herdando a regra global de `prefers-reduced-motion` sem lógica JS nova) e lista de recursos reais; formulário sem link "Esqueceu a senha?" (não existe) e sem citar perfis técnicos (`SUPER_ADMIN` etc.) na UI. Navegação cruzada nova entre `/login` e `/ativar-dispositivo`. Toda a lógica de autenticação (`login()`, `resolveHomeRoute`, proteção contra open redirect, `location.state.from`) ficou intocada — mudança puramente visual/de composição/roteamento. Ajuste feito durante a validação visual manual: painel do formulário passou de `--color-bg` para `--color-surface` porque, no tema dark, os dois painéis ficavam quase indistinguíveis | `frontend/src/components/auth/{AuthSplitLayout,LoginBrandPanel,FoodIcons}.tsx` (novos), `frontend/src/pages/auth/LoginPage.tsx`, `frontend/src/pages/AtivarDispositivoPage.tsx`, `frontend/src/routes/AppRoutes.tsx`, `frontend/src/pages/HomePage.tsx` (removido), `frontend/src/styles/{themes.css,global.css}`, `frontend/src/pages/auth/LoginPage.test.tsx` (ampliado), `frontend/src/routes/AppRoutes.test.tsx` (novo), `frontend/e2e/redesign-login.spec.ts` (novo), `frontend/e2e/admin-login.spec.ts` (heading atualizado), `docs/design-system/{componentes.md,cores.md}` | `npm test` → 149/149 (+7 novos); `tsc -b`/`npm run build` sem erro; `npx oxlint` limpo (mesmos 2 avisos pré-existentes); `npm run e2e` → 39/39 (9 novos); validação visual manual (capturas Playwright locais, dark/light, desktop/mobile) | `npm run e2e:integrado` não executado — mesma pendência de credenciais da TASK-116; esta task não toca contrato de backend algum |
| TASK-118 | ~~Redesign do dashboard e layout administrativo~~ **concluída** | UX/Produto | P2 | Usou a skill `ui-ux-pro-max`. Painel `/admin/*` ganhou layout persistente (`AdminLayout` = `AdminSidebar` expansível/recolhível/drawer mobile + `AdminTopbar`), substituindo o antigo `AppLayout` (título estático por página, sem navegação lateral) nas 7 telas; `AdminHomePage` (grade de módulos) e `AdminDashboardPage` (indicadores) fundidas numa só, servindo `/admin` (rota índice); `/admin/dashboard` passou a redirecionar. Dashboard mostra só dados reais de `GET /api/admin/dashboard` (nenhum número fictício); sidebar/hero reaproveitam os tokens `--color-auth-brand-*` da TASK-117, sem nenhuma cor nova. `AppRoutes.tsx` reestruturado em rotas aninhadas (`<Outlet/>`), `ProtectedRoute`/`RoleGuard` preservados sem alteração de regra. 3 bugs visuais reais encontrados e corrigidos na validação manual (overflow da topbar mobile, botão de recolher sidebar clicável dentro do drawer, contraste insuficiente da topbar no dark) | `frontend/src/components/layout/{AdminIcons,adminNav,AdminSidebar,AdminTopbar,AdminLayout}.tsx` (novos), `frontend/src/hooks/useAdminSidebarCollapsed.ts` (novo), `frontend/src/components/admin/{AdminDashboardHero,DashboardMetricCard}.tsx` (novos), `frontend/src/pages/admin/AdminDashboardPage.tsx` (reescrita, funde `AdminHomePage` removida), `frontend/src/routes/AppRoutes.tsx`, `frontend/src/auth/RoleGuard.tsx`, 6 páginas admin restantes (remoção de `AppLayout`/`AdminVoltarLink`, removido), `frontend/src/styles/{tokens.css,global.css}`, `frontend/e2e/admin-layout.spec.ts` (novo) + mocks de dashboard em 4 specs existentes, `docs/design-system/{componentes.md,cores.md}` | `npx tsc -b`/`npm run build` sem erro; `npm test` → 179/179 (+25 novos); `npx oxlint` limpo (mesmos 2 avisos pré-existentes); `npm run e2e` → 51/51 (12 novos, estável em 2 execuções completas); validação visual manual (dark/light, sidebar expandida/recolhida, mobile 320-1440px, drawer, `prefers-reduced-motion`, zoom 200%) | `npm run e2e:integrado` não executado — mesma pendência de credenciais das TASK-116/117; nenhum contrato de backend ou regra de autorização foi tocado. Recomendação de arquitetura registrada (não implementada): Caixa/Cozinha podem reaproveitar parcialmente a linguagem visual (tokens, `ThemeToggle`, identidade/logout do operador), sem o `AdminSidebar` completo, que não se encaixa no fluxo de toque rápido dessas telas |
| TASK-119.1 | ~~Corrigir sobreposição de dados nos cards de usuários~~ **concluída** | Bugfix/UX | P3 | `UsuarioCard` reaproveitava a grid compartilhada `dl.pedido-pendente-card__detalhes` (2-3 colunas, dimensionada para valores curtos) — e-mail/perfil longos extrapolavam a coluna e sobrepunham o campo vizinho. Corrigido com `dl.usuario-card__detalhes` própria (coluna única, `overflow-wrap: anywhere`), sem tocar a classe compartilhada nem outros cards | `frontend/src/components/admin/usuarios/UsuarioCard.tsx`, `frontend/src/styles/global.css`, `frontend/src/components/admin/usuarios/UsuarioCard.test.tsx` (novo), `frontend/e2e/usuario-card-layout.spec.ts` (novo), `docs/design-system/componentes.md` | `npx tsc -b`/`npm run build`/`npx oxlint` sem erro; `npm test` 211/211 (+8); `npm run e2e` 70/70 (+6); validação visual manual (4 perfis, e-mail/restaurante longos, sem restaurante, dark/light, mobile/desktop, zoom 200%) | Nenhuma — correção puramente visual, sem contrato/backend/dado alterado |
| TASK-119.2 | ~~Integrar o login do operador ao novo layout operacional~~ **concluída** | Bugfix/UX | P3 | `OperationalLayout`/`OperationalTopbar` (TASK-119) exigiam `operador` obrigatório — o intervalo real "dispositivo ativado, operador ainda não identificado" (o que se vê logo após ativar um Caixa/Cozinha) ficava preso na casca antiga (`AppLayout`/`ModuleHeader`), sem a nova topbar. Corrigido tornando `operador`/`onTrocarOperador` opcionais; sem operador a topbar mostra módulo/dispositivo/`ThemeToggle`/"Trocar dispositivo" e omite avatar/perfil/"Trocar operador"; `CaixaHomePage`/`CozinhaHomePage` fundiram os branches "sem operador"/"com operador" num único `OperationalLayout` (topbar nunca desmonta durante o login); `OperadorPainel` virou o próprio conteúdo central nesse estado (título/descrição/formulário, classes `.operational-login*` novas), perdendo `acaoTrocarDispositivo` (elimina duplicação — "Trocar dispositivo" é só da topbar agora) | `frontend/src/components/layout/{OperationalTopbar,OperationalLayout}.tsx`, `frontend/src/components/operador/OperadorPainel.tsx`, `frontend/src/pages/{caixa/CaixaHomePage,cozinha/CozinhaHomePage}.tsx`, `frontend/src/styles/global.css`, `frontend/e2e/painel-operacional.spec.ts` (ampliado) + `ativacao-dispositivo.spec.ts`/`operador-painel.spec.ts`/`fluxo-operacional-mock.spec.ts`/`login-centralizado.spec.ts` (ajustados), `docs/design-system/componentes.md` | `npx tsc -b`/`npm run build`/`npx oxlint` sem erro; `npm test` 225/225 (+14); `npm run e2e` 75/75 (+5), estável em 2 execuções completas; validação visual manual (Caixa/Cozinha sem operador dark/light, mobile 320/tablet 768, erro de credencial, transição sem→com operador, zoom 200%) | `npm run e2e:integrado` bloqueado (backend acessível, sem frontend/credenciais) — mudança exclusivamente de composição frontend, sem contrato tocado |
| TASK-119 | ~~Redesign dos painéis operacionais de Caixa e Cozinha~~ **concluída** | UX/Produto | P2 | Usou a skill `ui-ux-pro-max`. Executou a recomendação da TASK-118: `/caixa`/`/cozinha` ganharam `OperationalLayout`/`OperationalTopbar` (novos) — módulo, dispositivo, `ThemeToggle` e identidade do operador (avatar + perfil amigável) sempre visíveis, sem `AdminSidebar`; montada só quando dispositivo **e** operador já estão prontos, preservando os estados anteriores (`AppLayout centralizado` + `DispositivoAcessoCard`/`OperadorPainel`, inalterados). `OperadorPainel` simplificado — a exibição do operador identificado (antes embutida no fluxo de conteúdo) migrou para a topbar; `operador`/`onTrocar` removidos das props (código morto eliminado). Hierarquia dos cards da Cozinha reordenada (número→tempo→itens→observações→status→ação), com novo `formatarTempoDecorrido` mostrando só o tempo decorrido, sem classificar como atrasado/recente (nenhuma regra assim existe no backend). Novos `PedidoStatusBadge`/`OperationalEmptyState` reduzem duplicação entre Caixa/Cozinha sem fundir os cards (diferenças operacionais preservadas). Confirmado por leitura de código antes de implementar: **não existe polling** em Caixa/Cozinha hoje (atualização 100% manual) — nada a preservar/alterar nesse ponto | `frontend/src/components/layout/{OperationalIcons,OperationalTopbar,OperationalLayout}.tsx` (novos), `frontend/src/components/ui/{PedidoStatusBadge,OperationalEmptyState}.tsx` (novos), `frontend/src/pages/{caixa/CaixaHomePage,cozinha/CozinhaHomePage}.tsx`, `frontend/src/components/operador/OperadorPainel.tsx`, `frontend/src/components/{caixa/PedidoPendenteCard,cozinha/PedidoCozinhaCard,cozinha/ItemPedidoCozinhaRow}.tsx`, `frontend/src/utils/dateTime.ts` (`formatarTempoDecorrido`), `frontend/src/styles/{tokens.css,global.css}`, `frontend/e2e/painel-operacional.spec.ts` (novo) + `operador-painel.spec.ts`/`ativacao-dispositivo.spec.ts` ajustados, `docs/design-system/{componentes.md,cores.md}`, `docs/checklists/fluxo-operacional-mvp.md` | `npx tsc -b`/`npm run build`/`npx oxlint` sem erro (mesmos 2 avisos pré-existentes); `npm test` → 203/203 (+28 novos/ampliados); `npm run e2e` → 64/64 (13 novos), estável em 2 execuções completas; validação visual manual (dark/light, mobile 375/tablet 768/desktop 1920, zoom 200%, textos longos) | `npm run e2e:integrado` **bloqueado** — backend real acessível (`/actuator/health` 200, verificado sem iniciar nada), mas sem frontend rodando em 5173/5174 nem credenciais `E2E_ADMIN_*` disponíveis; nenhum contrato/autenticação/autorização foi tocado, risco de regressão nulo. **1 bug real de UX encontrado e corrigido na validação visual**: "Trocar operador"/"Trocar dispositivo" usavam o mesmo ícone genérico, indistinguíveis em mobile (só ícone visível) — corrigido com dois ícones específicos |

## 7. Tarefas que devem ser agrupadas ou separadas

- **PIN de operador deve ficar separado de auditoria por operador** (TASK-107): PIN é UX de login, auditoria é relatório — não têm dependência técnica entre si e misturar as duas amplia o escopo de uma task pequena.
- **WebSocket deve vir depois de estabilizar a base de testes** (Vitest + Playwright, TASK-101/102), não antes — mudar a forma como Caixa/Cozinha recebem atualização sem nenhum teste automatizado de frontend é o tipo de mudança que historicamente já gerou regressão neste projeto (ver TASK-064, condição de corrida em refresh token descoberta só por validação manual).
- **Rede/Grupo de restaurantes deve vir antes de qualquer produção multi-restaurante real**, mas não antes disso — é uma mudança de modelo de dados (nova entidade, nova hierarquia de escopo) que não deve ser antecipada sem um cliente real pedindo múltiplos restaurantes sob uma mesma rede. Priorizá-la agora seria construir para um requisito hipotético.
- **Testes de frontend (Vitest) devem vir antes de qualquer refatoração ou feature nova de frontend** (WebSocket, dashboard de operador) — não antes de segurança (P0), que continua tendo prioridade absoluta.
- **E2E (Playwright) deve vir depois de Vitest**, não em paralelo: Vitest é mais barato de manter e cobre a lógica de sessão/tokens que é a parte mais sensível do frontend; só depois disso estabelecido vale investir no custo maior de manutenção de testes E2E.
- **JWT secret (TASK-097) e CORS (TASK-098) podem ser feitas na mesma leva de PRs que a seed do SUPER_ADMIN (TASK-096)** — as três mexem em `SecurityConfig`/`application.yml`/migrations de forma isolada e são pequenas o suficiente para não precisar de uma task por semana; agrupá-las numa "sprint de hardening pré-produção" é razoável, desde que cada uma continue sendo um commit/PR isolado e testado.

## 8. Riscos de mexer agora

- **Alterar `JwtService`/`SecurityConfig` logo após a introdução do operador (TASK-092)** pode gerar regressão silenciosa no fluxo de operador, que depende de claims específicos (`tipo=OPERADOR`, `operadorId`, `perfil`, `restauranteId`, `dispositivoId`) — qualquer mudança em `JwtService` deve rodar a suíte completa (320 testes) antes de mesclar, não só os testes de autenticação de usuário.
- **Implementar PIN de operador exige migration** (nova coluna/tabela para hash de PIN) e reabre decisões já tomadas na TASK-091/092 (email/senha, sem PIN) — não é uma mudança "pequena e isolada", é uma revisão de decisão arquitetural.
- **WebSocket muda a arquitetura de atualização** de um modelo request/response simples (polling) para conexões de longa duração — impacta autenticação (como autenticar uma conexão WebSocket com o mesmo token de dispositivo?), infraestrutura (sticky sessions se houver múltiplas instâncias) e testes (Testcontainers/integração precisam de nova abordagem). Não é um ajuste de service, é uma mudança de plataforma.
- **Multi-tenant por rede muda o modelo de escopo** que hoje é auditado e testado exaustivamente por `restauranteId` (`AdminScopeService`, presente em Categoria/Produto/Dispositivo/Usuário/Pedido/Dashboard) — introduzir uma camada de "rede" acima de restaurante exige revisar escopo em todos esses módulos simultaneamente, sob risco de abrir brecha de acesso cruzado entre redes se algum módulo for esquecido.
- **Pagamentos reais mudam regras financeiras**: estorno, conciliação, chargeback e auditoria fiscal são responsabilidades que hoje não existem no sistema (payment é 100% simulado) — introduzir um gateway real sem antes desenhar essas regras é o tipo de mudança que não deve ser feita como "só trocar o provider".

## 9. Recomendação: não fazer agora

- **Entidade Rede/Grupo de restaurantes e multi-tenant por rede** — nenhuma evidência de demanda real de negócio para múltiplos restaurantes sob uma mesma rede; implementar agora seria construir para um requisito hipotético, indo contra o princípio de não criar abstração antes da necessidade.
- **PIN de operador** — melhoria de UX real, mas exige reabrir uma decisão arquitetural recente (TASK-091/092) e uma migration, sem que exista hoje uma reclamação concreta de que email/senha seja lento demais na prática (ainda não homologado visualmente).
- **Gateway de pagamento real (Pix/TEF/SmartPOS)** — mudança financeira e regulatória grande demais para ser uma "próxima task"; deve ser seu próprio épico, com decisão de produto e de compliance antes de qualquer código.
- **WebSocket/SSE** — antes de existir qualquer teste automatizado de frontend, trocar a estratégia de atualização das telas mais críticas (Caixa/Cozinha) é assumir risco de regressão sem rede de segurança.
- **Estorno automático de pagamento no cancelamento** — hoje é decisão de produto deliberada (documentada desde a TASK-024), não um bug; misturar isso com pagamento real faz mais sentido do que resolver isoladamente com o `FakePaymentProvider` atual.
- **Backup/restore automatizado e deploy/CD completo** — fazem sentido quando houver um ambiente de produção real definido (provedor de nuvem, banco gerenciado); não há hoje uma infraestrutura de produção contra a qual desenhar isso, então seria trabalho especulativo.

## 10. Itens obrigatórios antes de produção

- ~~Seed de `SUPER_ADMIN` sem senha fixa pública~~ **concluído na TASK-096**.
- ~~`JWT_SECRET` obrigatório, sem fallback~~ **concluído na TASK-097** (obrigatório inclusive em dev, não só fora de um profile de produção).
- ~~CORS configurável por ambiente, sem `localhost` hardcoded~~ **concluído na TASK-098**.
- Definir estratégia de storage de upload não-local se houver mais de uma instância da aplicação.
- Definir estratégia de rate limiting compatível com múltiplas instâncias (hoje em memória, por instância).

## 11. Itens recomendados antes de demo

**Visual/demo**: nenhuma alteração de código necessária — o painel Admin já foi clicado de verdade (TASK-086); o fluxo Totem/Caixa/Cozinha/operador nunca foi, então uma demo ao vivo deve ser ensaiada manualmente antes de qualquer apresentação, já que a validação existente é só funcional (`curl`), não visual.

**Estabilidade**: rodar `mvn test` e `npm run build`/`npx oxlint` imediatamente antes da demo (estado atual, sem alteração nesta task: 320/320 backend, build/lint frontend limpos — ver seção de validações abaixo).

**Dados seed**: revisar se o restaurante/cardápio/dispositivos usados na demo são dados de demonstração apresentáveis (nome, imagens, preços), não dados de teste como "Restaurante Teste 069".

**Roteiro de apresentação**: seguir o fluxo documentado em `docs/11-fluxos.md` ("Fluxo operacional completo") como roteiro literal de clique, incluindo o fluxo de operador (`docs/11-fluxos.md` "Fluxo de identificação de operador").

**Checklist de ambiente**: backend + PostgreSQL + frontend (`npm run dev`) rodando; ao menos um dispositivo de cada tipo (TOTEM/CAIXA/COZINHA) já ativado; ao menos um operador de cada perfil cadastrado, para não precisar cadastrar nada ao vivo.

**Riscos que devem ser comunicados** à audiência antes/durante a demo: pagamento é simulado (`FakePaymentProvider`, sem Pix/TEF real); esta é uma demonstração de fluxo, não de infraestrutura de produção (CORS/JWT/seed ainda não endurecidos — seção 10); nenhuma validação de clique real foi feita no fluxo Caixa/Cozinha/operador antes desta apresentação, então bugs de UX (não de backend) são mais prováveis ali do que no Admin.

## Validações executadas nesta revisão

Task estritamente documental — nenhuma alteração de `backend/src`/`frontend/src`. Não foi necessário reexecutar a suíte completa: os resultados mais recentes (TASK-094.1, mesma sessão, sem nenhuma alteração de código desde então) permanecem válidos — `mvn test` → 320/320 BUILD SUCCESS; `npm run build` sem erro; `npx oxlint` sem erro (warning cosmético pré-existente em `ThemeContext.tsx`). Fatos de código citados neste roadmap (CORS hardcoded, JWT secret com fallback, seed de senha fixa, ausência de Actuator/Dockerfile/Vitest) foram confirmados por leitura direta do código nesta revisão, não presumidos a partir da documentação.

## Próxima task recomendada

~~TASK-096 — Seed de `SUPER_ADMIN` seguro~~ **concluída** (ver `docs/status-mvp.md`/`docs/testes-backend-mvp.md` seção 7-tredecies).

~~TASK-097 — JWT secret sem fallback inseguro~~ **concluída** (ver `docs/status-mvp.md`/`docs/testes-backend-mvp.md` seção 7-quattuordecies).

~~TASK-098 — CORS externalizado por ambiente~~ **concluída** (ver `docs/status-mvp.md`/`docs/testes-backend-mvp.md` seção 7-quindecies).

**A leva de hardening P0 da TASK-095 está completa** — os três riscos (seed de SUPER_ADMIN, JWT secret, CORS) foram corrigidos nas TASK-096/097/098, todos seguindo o mesmo padrão (variável de ambiente obrigatória, validação explícita no startup, sem fallback de produção commitado).

~~TASK-099 — Observabilidade mínima (Actuator + logs operacionais)~~ **concluída** (ver `docs/status-mvp.md`/`docs/testes-backend-mvp.md` seção 7-sedecies) — `/actuator/health`/`/actuator/info` públicos, demais endpoints do Actuator não expostos, logs operacionais completos nos fluxos sensíveis.

**TASK-100 (branch protection + badge de CI) documentada** — badge no `README.md`, checklist completo em `docs/ci-branch-protection.md`; falta só a configuração manual da regra no GitHub (Settings → Branches), que exige acesso administrativo e fica fora do escopo de uma task de código/documentação. **TASK-101 (Vitest + Testing Library) concluída** — 37 testes cobrindo `tokenStorage.ts`, `dateTime.ts`, `adminScope.ts`, `OperadorPainel.tsx` e a renovação de sessão em `api.ts`; `npm test` rodando no CI. **TASK-102 (Playwright headless) concluída** — 7 testes E2E mockados (login Admin, `OperadorPainel`, Totem/Caixa/Cozinha), fechando em parte a pendência histórica de "clique real" (TASK-094.1); achou e corrigiu um bug real de CSS no layout desktop do Totem. **TASK-103 (Playwright no CI) concluída** — job `Frontend E2E (Playwright)` em `ci.yml`, `needs: frontend`, artifact de relatório só em falha. **TASK-103.1 (validação do primeiro run real) concluída — tudo verde**: os 4 jobs passaram no run `29361958515` (commit `0f98475`), incluindo `Frontend E2E (Playwright)` (~40s, 0 artifacts gerados, como esperado num run sem falha); nenhuma correção de código foi necessária. Recomendação: acompanhar mais 1-2 runs antes de promover o check a obrigatório em branch protection. **TASK-104 (E2E integrado com backend real — Totem) concluída** — primeira suíte E2E sem mocks (`frontend/e2e-integrado/`), Fluxo A (Totem) validado de ponta a ponta contra PostgreSQL+backend reais locais, 1/1 passou de primeira. **TASK-105 (E2E integrado — Caixa/Cozinha/operador) concluída** — segundo spec integrado, dois `BrowserContext` simulando terminais reais, pedido `PAGO`→`RETIRADO` por clique real com operadores reais, histórico de auditoria validado via API; 2/2 (os dois specs juntos) passou de primeira. **TASK-106 (E2E integrado no CI) concluída** — job `Frontend E2E Integrado (Backend real)` sobe PostgreSQL (`services:`) e o backend real (`mvn spring-boot:run`) no próprio runner; validado localmente reproduzindo o mesmo padrão antes do push; ainda não é check obrigatório. **Próxima: acompanhar os primeiros runs reais deste job no GitHub Actions** (equivalente à TASK-103.1, mas para o integrado) e, se estável, considerar promovê-lo; alternativa/complementar: encadear um fluxo Totem→Caixa→Cozinha com o mesmo pedido na mesma execução.

**TASK-110 (Refatoração visual do login e cadastros administrativos) concluída** — login centralizado, `Modal.tsx` reutilizável e acessível, e as 5 páginas administrativas (Restaurantes/Dispositivos/Categorias/Produtos/Usuários) migradas para o padrão lista-primeiro + botão "Novo..." + modal, sem tocar backend/contrato/permissões; `npm test` 47/47, `npm run build`/`npx oxlint` limpos, `npm run e2e` 7/7 sem regressão. **Próxima recomendada**: estender o teste de página (`*.test.tsx` no padrão de `AdminRestaurantesPage.test.tsx`) para as 4 páginas restantes, e considerar um spec Playwright mockado cobrindo o fluxo "Novo → modal → salvar" de pelo menos uma tela Admin.

**TASK-111 (Bloquear Caixa e Cozinha até login do operador) concluída** — fechou uma lacuna de segurança real: dispositivo CAIXA/COZINHA ativado, sozinho, mostrava a fila operacional inteira antes de qualquer operador se identificar. Frontend (`CaixaHomePage`/`CozinhaHomePage`) só busca dados com operador identificado; backend (`OperadorContextService.resolverObrigatorio`, novo) passou a exigir `X-Operador-Token` nas duas listagens, não confiando só na ocultação do frontend. `mvn test` 353/353, `npm test` 53/53, `npm run build`/`npx oxlint` limpos, `npm run e2e` 7/7. **Não executado nesta task**: `npm run e2e:integrado` (o backend local já estava em uso para outro fim durante a sessão; os specs integrados de Caixa/Cozinha já logavam operador via UI antes desta mudança, então a expectativa é que continuem passando sem alteração — recomenda-se confirmar isso na próxima oportunidade em que o ambiente integrado estiver livre).

**TASK-112 (Recuperar ativação de dispositivo nas telas operacionais) concluída** — corrigiu um efeito colateral não intencional da TASK-111: sem dispositivo, `/caixa`/`/cozinha` só redirecionavam silenciosamente (sem botão) e não havia forma de trocar de equipamento pela tela depois de ativado. `DispositivoAcessoCard` (novo) cobre os estados "sem dispositivo"/"incompatível"; `OperadorPainel` ganhou "Trocar dispositivo" (com confirmação); `useDispositivoOperacional` (novo hook) tornou `dispositivo` estado React reativo, corrigindo o risco de tela presa em conteúdo antigo após sessão invalidada. Investigado e confirmado sem gap: `AtivarDispositivoPage` e `TotemHomePage` já satisfaziam os requisitos. `npm test` 66/66, `npm run build`/`npx oxlint` limpos, `npm run e2e` 10/10. Backend não alterado. **Não executado**: `npm run e2e:integrado` (mesmo motivo da TASK-111 — backend local em uso para outro fim, sem credencial de SUPER_ADMIN conhecida para essa instância). **Próxima recomendada**: quando o ambiente integrado estiver livre, rodar `npm run e2e:integrado` das TASK-111/112 juntas para fechar essa pendência acumulada.

**TASK-113 (Acessibilidade visual e touch targets) concluída** — skill `ui-ux-pro-max` usada para revisar a interface; corrigiu 4 categorias: contraste (`--color-primary-button` novo token só para fundo preenchido, `--color-text-muted` no tema claro, migração de 2 textos reais que usavam `--color-text-soft`), touch targets (`.theme-toggle`/`.ui-modal__fechar`/`.cart-item-row__qtd-botao` de 28-40px para 44px), foco (`:focus-visible` unificado) e `prefers-reduced-motion` (não existia). Decisão documentada: preferiu criar um token novo a escurecer `--color-primary` diretamente, porque isso pioraria seu uso como texto (medido, não só suposto). Zero mudança de componente/lógica, zero regressão (`npm test` 66/66 sem nenhum TSX tocado). **Não feito nesta task, deliberadamente**: erro inline por campo nos formulários administrativos, e o contraste de `--color-primary` usado como **texto** (preços/links/badges, ainda abaixo de 4.5:1 em vários lugares) — ambos ficam como próxima recomendação.

**TASK-114 (Padronização visual de botões, ícones e componentes) concluída** — skill `ui-ux-pro-max` usada para auditar a interface. Consolidou 5 classes de "botão fantasma" duplicadas em `Button.tsx` (`variant`/`fullWidth`/`aria-busy`), corrigindo de brinde o touch target de `operador-painel__trocar` (bug real que a TASK-113 não cobria); deduplicou a badge `.pedido-pendente-card__status` contra `.dispositivo-card__status`; padronizou `aria-hidden`/tamanho dos 3 ícones-glifo do projeto. `npm test` 74/74, `npm run build` (CSS 36,55kB→35,17kB), `npx oxlint` limpo, `npm run e2e` 17/17. Um teste Playwright novo pegou uma regressão real de padding (scroll horizontal em mobile) introduzida durante a própria task, corrigida antes de finalizar — evidência de que a validação de responsividade pedida na task tem valor real, não é formalidade. **Próxima recomendada**: erro inline por campo nos formulários administrativos (adiado por 2 tasks seguidas, já a pendência mais antiga do roadmap de UX) e revisão do contraste de `--color-primary` usado como texto (preços/links/badges, ainda abaixo de 4.5:1 em vários lugares nos dois temas).

**TASK-115 (Validação inline nos formulários administrativos) concluída** — fechou a pendência mais antiga do roadmap de UX (adiada nas TASK-113/114). `Input.tsx` ganhou `forwardRef`+`error`/`helpText`; novo `FieldError.tsx` (`aria-live="polite"`); os 5 formulários (Restaurante/Dispositivo/Categoria/Produto/Usuário) passaram a validar todos os campos no submit, revalidar só o campo já inválido ao corrigir, e focar automaticamente o primeiro campo inválido (`focarPrimeiroErro`, refs explícitos por formulário, nunca `querySelector` global). Achado: o backend já retornava erro de campo estruturado (`ApiErrorResponse.errors[]`) nunca consumido pelo frontend — `extrairErrosCampoApi` passou a mapear isso para inline sem adivinhação textual, mantendo a mensagem global só para erro de API sem campo reconhecido/falha de comunicação. Zero mudança de backend/contrato/regra de negócio. `npm test` 104/104 (+30 novos), `npm run build`/`npx oxlint` limpos, `npm run e2e` 20/20 (17 anteriores + 3 novos). **Não executado**: `npm run e2e:integrado` — nenhum nome de campo/contrato mudou, só a apresentação do erro, então a expectativa é que continue passando sem alteração; recomenda-se confirmar na próxima janela com backend disponível.

**TASK-116 (Validação e homologação da autenticação centralizada) concluída** — a centralização de login (`AuthProvider`/`ProtectedRoute`/`RoleGuard`/`resolveHomeRoute`, storage separado USER/DEVICE/OPERADOR) tinha sido implementada em código sem nenhum build/teste/lint/E2E jamais executado; esta task rodou tudo de verdade. Corrigiu a única regressão real encontrada (`e2e/acessibilidade.spec.ts` quebrado pelo redirect client-side de `/admin/login`) e um achado de segurança real do `JwtAuthenticationFilter` (token `OPERADOR` conseguia autenticar como `USER` via `Authorization: Bearer`, agora rejeitado). `mvn test` 354/354, `npm test` 142/142 (+30 novos cobrindo isolamento de sessão), `npm run build`/`npx oxlint` limpos, `npm run e2e` 30/30. **Não executado**: `npm run e2e:integrado` — sem credenciais `E2E_ADMIN_EMAIL`/`PASSWORD` confirmadas para o backend em execução.

**TASK-117 (Redesign do login e entrada padrão pela ativação de dispositivo) concluída** — usou a skill `ui-ux-pro-max`. `/` passou a redirecionar para `/ativar-dispositivo`; `/login` ganhou layout dividido com painel institucional "TotemFood" (`AuthSplitLayout`/`LoginBrandPanel`/`FoodIcons`, os três primeiros SVGs decorativos do projeto) e navegação cruzada nova entre as duas rotas. Toda a lógica de autenticação ficou intocada — mudança puramente visual/de composição/roteamento. `npm test` 149/149 (+7), `tsc -b`/`npm run build`/`npx oxlint` limpos, `npm run e2e` 39/39 (+9), validação visual manual dark/light/desktop/mobile. **Não executado**: `npm run e2e:integrado` (mesma pendência de credenciais).

**TASK-118 (Redesign do dashboard e layout administrativo) concluída** — usou a skill `ui-ux-pro-max`. Deu continuidade visual às TASK-116/117: painel `/admin/*` ganhou um layout persistente (`AdminLayout` = `AdminSidebar` expansível/recolhível/drawer mobile + `AdminTopbar`), reaproveitando os tokens `--color-auth-brand-*` do login (nenhuma cor nova); substituiu o antigo `AppLayout` (título estático, sem navegação lateral) nas 7 telas administrativas; `AdminHomePage`+`AdminDashboardPage` fundidas numa só (rota índice `/admin`), mostrando só indicadores reais de `GET /api/admin/dashboard` (nenhum número fictício, ao contrário da referência visual fornecida na task). `AppRoutes.tsx` reestruturado em rotas aninhadas (`<Outlet/>`) sem alterar nenhuma regra de `ProtectedRoute`/`RoleGuard`. `npx tsc -b`/`npm run build`/`npx oxlint` limpos, `npm test` 179/179 (+25), `npm run e2e` 51/51 (+12, estável em 2 execuções completas), validação visual manual (dark/light, sidebar expandida/recolhida, mobile 320-1440px, drawer, `prefers-reduced-motion`, zoom 200%) encontrou e corrigiu 3 bugs reais (overflow da topbar mobile, botão de recolher sidebar clicável dentro do drawer, contraste insuficiente da topbar no dark). **Não executado**: `npm run e2e:integrado` (mesma pendência de credenciais das TASK-116/117; esta task não toca contrato de backend nem regra de autorização). **Próxima recomendada**: aplicar parcialmente a linguagem visual deste layout (tokens `--color-auth-brand-*`, `ThemeToggle`, um componente de identidade/logout do operador) a Caixa/Cozinha, **sem** forçar o `AdminSidebar` completo — essas telas são de toque rápido, com necessidades de interação diferentes de um painel de gestão; considerar também implementar `GET /api/auth/me` (avaliado na TASK-116, ainda não implementado).

**TASK-119 (Redesign dos painéis operacionais de Caixa e Cozinha) concluída** — usou a skill `ui-ux-pro-max`. Executou exatamente a recomendação da TASK-118: `/caixa`/`/cozinha` ganharam `OperationalLayout`/`OperationalTopbar` (novos), reaproveitando o vocabulário visual da `AdminTopbar` (avatar textual, ações com `aria-label`+texto ocultável em mobile) **sem** reaproveitar o componente em si e **sem** `AdminSidebar` — módulo, dispositivo, `ThemeToggle` e identidade do operador (perfil amigável, nunca o enum técnico) sempre visíveis, montados só quando dispositivo e operador já estão prontos (os estados anteriores continuam com `AppLayout centralizado`, inalterados). `OperadorPainel` simplificado — a exibição do operador identificado migrou para a topbar, `operador`/`onTrocar` removidos das props (código morto eliminado). Cards da Cozinha reordenados (número→tempo→itens→observações→status→ação), com `formatarTempoDecorrido` novo mostrando só o tempo decorrido, sem inventar uma regra de "atrasado" que não existe no backend. Confirmado por leitura de código antes de implementar que **Caixa/Cozinha nunca tiveram polling** — atualização continua 100% manual, nada alterado nesse ponto. `npx tsc -b`/`npm run build`/`npx oxlint` limpos, `npm test` 203/203 (+28), `npm run e2e` 64/64 (+13), estável em 2 execuções completas, validação visual manual (dark/light, mobile 375/tablet 768/desktop 1920, zoom 200%, textos longos) encontrou e corrigiu 1 bug real: "Trocar operador"/"Trocar dispositivo" usavam o mesmo ícone genérico de troca, indistinguíveis em mobile onde só o ícone fica visível — corrigido com `TrocarOperadorIcon`/`TrocarDispositivoIcon` específicos. **Não executado**: `npm run e2e:integrado` — backend real acessível (verificado sem iniciar nada), mas sem frontend rodando nem credenciais `E2E_ADMIN_*` disponíveis; nenhum contrato/autenticação/autorização foi tocado. **Próxima recomendada**: nenhuma pendência funcional nova desta task; quando o ambiente integrado estiver disponível, rodar `npm run e2e:integrado` das TASK-111/112/119 juntas fecharia a pendência acumulada desde a TASK-112 de validar o fluxo operacional completo contra backend real.
